# Workday development — September 10, 2026

## Authorization and deadline

The user accepted the previous checkpoint, requested its merge and release, then authorized continued development on a new branch while at work until **15:00 Europe/Copenhagen / 13:00 UTC today**. The accepted baseline is public **v0.6.3**, commit `922a4b576165f6af8477f69bc4c41a48f64916b1`; its release export and main regression workflow passed.

Branch: `codex/encounter-evolution-20260910`, created from that clean baseline. Keep `main` and release tags at the accepted version. Useful game improvements, bounded additions and code cleanup remain authorized. Stop adding features at **13:30 local**; reserve the remaining time for fixes, verification and delivery. Do not use the entire time box as a reason to add weak or unverified scope.

The existing task continuation `descent-identity-development-until-06-00` has been updated for this workday: every 20 minutes until 13:00 UTC. Resume current conversation, Git and agent state rather than restarting work. Pause it at final delivery or the deadline.

## First milestone — Encounter decisions and feedback

**Encounter decisions and feedback.** Shatterfield Crossfire keeps its existing original/mirrored four-column formations, but the two inner columns become cracked cover. Three separate deliberate Attack contacts break a cracked column, opening a crossing lane while removing that shelter from archers. The two outer columns remain solid. This is a bounded design hypothesis to playtest, not a balance conclusion drawn from telemetry.

- Existing melee, charged Blast and the same Attack's Razor Wind geometry can contact cover; a given action contacts a given column at most once. Fields, echoes, returning blades, other descendants, enemy damage and ordinary movement do not break it. Cover never counts as a damaged enemy, successful enemy hit, kill or resource generator.
- Untouched cracked cover looks different from solid cover, further cracks and three small marks show remaining contacts, and sparse flat rubble leaves an obvious open center after destruction. A short existing room-entry subtitle explains the action. Geometry, marks and visual feedback must agree.
- Host authority owns contacts and broadcasts consistent state, rejecting stale run/room updates and duplicate actions. Destruction removes the collider, Orbit anchor and spawn exclusion together. Existing saved profiles without the new optional metadata stay solid; Continue preserves offered profile metadata through the existing between-room checkpoint.
- Acceptance: both formations preserve their four initial radius-28 columns; only the inner two break after exactly three eligible actions; all direct shape variants, non-eligible descendants and action limits behave correctly; opened lanes work physically and on both ENet peers; room/retry/Continue clear or restore the appropriate state; actual camera captures distinguish intact, damaged and open cover. Human playtest must establish whether shelter versus space is an interesting choice.

Supporting correction: Pulse Window currently hides the active rule and duration after its banner, and replicas omit the modifier needed by the HUD. Carry that existing state through the HUD and sync contract, show the rule and remaining active time, and return to the next-pulse countdown on expiry. Preserve all host pulse timing, roster selection and mechanics; client countdown is presentation only and respects combat pause/grace.

**Independent tooling increment:** extend normal exported-build validation to exercise native startup and menu flow in isolation. An inert export from the exact Godot release template proved that an adjacent `override.cfg` can add an external test controller and configure isolated user data before autoload startup while keeping the executable byte-identical. The real smoke will accept only a preflight-verified normal `dev-*` build, disable all service endpoints/credentials, use a unique temporary copy and retain the packaged Menu/Main and disabled debug settings. The existing package probe stays available. Report the external driver and configuration overrides explicitly; this does not prove internet co-op or human enjoyment.

## Ownership

- `encounter_opportunities`: cover controller, player action integration, world authority/replication, anchor cleanup and behavioral/network tests.
- `buildcraft_opportunities`: Pulse Window manager/HUD/coordinator correction and focused behavioral/rendered verification.
- `pipeline_audit`: optional normal native executable smoke and its external fixture/preflight.
- Root: authored cover metadata, renderer and GPU fixture, Pulse Window world HUD forwarding, documentation, independent review coordination and delivery.

Agents do not commit independently. Coordinate shared file ownership. Run all automation in disposable project copies with isolated user data through existing helpers. Preserve combat wording, established controls/visual style, save compatibility and host authority. Keep development telemetry and leaderboards local.

## Delivery

Use scoped checks while iterating, applicable actual rendered/network evidence, and the full required suite at coherent checkpoints. Commit and non-force push the development branch. Deliver a normal build through `.github/scripts/export_playtest.ps1` without `-DebugRun` to the single `C:/Users/mikel/Desktop/AbyssalDescent Playtest.exe`; verify package configuration, source identity and final hash. Avoid replacing an actively played build for intermediate experiments. Record what was implemented and verified separately from human acceptance.

## Progress

- 07:44 local: new branch created; continuation updated; three independent opportunity audits running. Root is reviewing run/menu persistence and updating the accepted baseline in the development plan.
- Scope selected: brittle inner cover in Shatterfield Crossfire and the reproduced missing Pulse Window display, with native normal-build validation as the independent tooling increment. Implementation is underway; the desktop build remains the accepted prior checkpoint.
- Cover implementation passes 99 focused checks after full script compilation and world/network guards. Actual Main instances cover all four characters, three contacts and real collision gaps, melee/Wind/Blast, stale actions and duplicate suppression, no enemy-hit resources or stats, saved-profile immutability, legacy solid cover, Orbit detach and next-room state. Review caught and fixed immediate group removal before detach and stale-run pending-state replacement.
- Root inspected all 11 cover GPU frames (81 checks): both formations at all four contact states, intact/damaged cover amid party effects, and 960px intact/open views. Separate arrays keep rubble out of physics/spawn exclusions. A subsequent renderer guard exactly matches the controller's integer-three metadata requirement without changing valid rendered states.
- Pulse checks cover actual host state, replica countdown and expiry, legacy payloads, Pause/Build layering and HUD reuse. GPU review passes six frames/21 checks at 960 and 1280; it also fixed the status card retaining empty height after the active rule expires.
- Native normal-executable validation passes 62 native checks plus the existing 27 package checks on the accepted prior development artifact. It uses injected Enter/Escape input and movement actions, keeps the executable hash unchanged, and exercises normal first-launch prompts, tutorial and a second Main instance. The initial test expected depth one; normal tutorial correctly begins at zero. Explicit final scene/audio/cache retirement produces no shutdown leak warnings. Public/debug rejection, changed-copy/sidecar rejection, isolated preflight failures and PowerShell 5.1 parsing have separate bounded guard evidence. The candidate export must receive its own native run before delivery.
- Final dedicated ENet passes 90 host/93 client checks, including fresh-sequence invalid requests, wrong-run state, both owners' real concurrent Attacks plus replays, joining-player destruction, Orbit release, old solid layouts, next-room reset and actual Pulse RPC/pause/expiry. Final scoped Pulse/pause checks pass 64/28 assertions with all 308 scripts compiling. Independent authority, metadata, rendering and Pulse reviews have no actionable findings. Existing client input ownership remains; this is not a new anti-cheat architecture.

## Verification and handoff

The first bounded implementation is committed and pushed as `87c96346e4e98c6a105c8201ee0c093697595a85`. Its required pre-commit suite passed all 308 scripts and 73 gameplay fixtures, and [hosted CI passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34443642073). The normal candidate export passed package inspection and 62 native startup/menu checks. No replacement desktop build has been delivered yet; preserve the accepted desktop build until final workday delivery.

- Full checkpoint suite: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-ddb23ac82aad4ede995b69d209adc7f3`.
- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-workday-candidate-12452527e2764282b74426795c97215a/AbyssalDescent.exe`.
- Candidate build ID: `dev-20260910-060049822-bbca5fdc`; SHA256: `AF9013F34525248DE51489B1645EFF2CB0E08B99A82242FF1BE5667112DF153D`; 114243600 bytes.
- Candidate native report: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-c9e11b10549f40b58310ae38bc253cc1/report.json`.
- Source identity: 520 of 522 production files match exactly; independently checked the two expected exporter overrides (development build ID and Main debug disabled). Details retained in candidate `source-identity.json`.

- Cover native: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-0907d81aaa5a45f986b04dbedc48d14c`.
- Cover GPU: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-f1d88009a52148718df0ebe602d07a8a/brittle_cover_frames`.
- Pulse GPU: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-6e0716c88c854d50a90221372a52a697/pulse_hud_frames`.
- Final Pulse/pause scoped run: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-71619527334e462eb1d1d72ac5e9a92e`.
- Dedicated real ENet: `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-54c2190fec23490a810105c1389a519f`.
- Native normal executable proof on prior accepted dev build: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-523c4956b56f464baff028dbd81a705d/report.json`.
- Native guard proof: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-guard-tests-7eff6138eafe4f7cbd6c959d5da0d6ea/`.

Remaining human questions: whether preserving shelter versus opening a lane is useful with ordinarily earned builds, whether the cracked-cover action is discoverable, and whether the Pulse Window rule is readable during a live fight. Local staged ENet and automated tutorial flow do not establish full internet-lobby play or balance.

## Second milestone — Continue reliability

The current checkpoint writer truncates the live save before checking whether replacement succeeded. Menu and world callers also ignore write/clear failure and can silently enter a fresh descent after a failed resume. Improve this bounded active-run store and its callers; other profile/history/settings stores remain outside this milestone.

- Prepare and reopen-verify a candidate and recovery record before replacing primary contents. Recover only an existing, demonstrably invalid primary. A valid primary wins; unreadable/future-format records are preserved. An absent primary is authoritative clear, because older release builds delete only that file. Temporary files never resume. Retire recovery after successful publish/read; never truncate the sole valid recovery copy while preparing another write.
- Keep the version-one envelope and existing normal/debug filenames. Clear succeeds only when the primary is absent or a checked cleared record prevents resuming. Check each operation's result and distinguish missing, invalid, unsupported and I/O failures.
- Menu revalidates Resume at activation and gates new runs on successful clear; errors offer Retry and an explicit error-only Discard saved descent action. A failed resume during Main boot returns to this error state without starting a tutorial or fresh run. No automatic discard of invalid saves.
- Failed voluntary Abandon/Retry/results-menu actions keep the current screen and state. Natural death/victory still finalize once, with a visible storage notice; departure retries clear. Failed doorway saves show a progress warning without promising an earlier checkpoint exists. Co-op callers never touch the suspended solo checkpoint or its resume request.
- `encounter_opportunities` owns the store/delegation and file-failure tests; `buildcraft_opportunities` owns menu/error UI and acceptance; `pipeline_audit` reviewed the protocol and owns lifecycle failure tests; root owns world gating, pause/results notice UI, rendered verification and documentation. Only root registers the new fixtures in the central runner and commits.

This is checked interrupted-write recovery, not an atomic replacement or a hardware power-loss guarantee. Godot 4.6.2's Windows rename removes an existing destination first and its flush uses `fflush`; the implementation therefore verifies reopened contents and retains the primary directory entry. Missing-primary recovery would be incompatible with an older build's deliberate clear. Switching to an older build between an unresolved recovery-cleanup failure and that build's own interrupted new save remains ambiguous; do not claim universal cross-version crash recovery.

Second-milestone implementation is frozen for its full checkpoint check. Independent review found and fixed a sole-backup hazard: unreadable/unsupported recovery now blocks overwrite, and an already-resolved sole recovery is never reread or rewritten during preparation. Review has no remaining findings. UI review also fixed menu actions extending below the viewport, centered Pause resizing during an active entrance animation, and Victory's Escape route opening Pause around result clear gating.

- Store: 72 checks, all 314 scripts and property/network guards pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-d9ef6d6e615544fab0786d9178f1e98e`.
- Menu: 68 new checks plus 183 existing layout/focus checks; `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-d349d0e41561426db2d74e0512cead54`.
- Lifecycle: 159 checks, including actual caller signals, failed doorway save and later recovery, paused Abandon, retry setup ordering, terminal outcomes, failed boot and co-op isolation; `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-c1b0909342a740b2b30e881db334fbdc`.
- Menu GPU: root and owner inspected both 960/1280 error frames and every action, including Exit, fits; `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-3eb2446da7de402b8695dcbf34066507/checkpoint_menu_frames`.
- Pause/results GPU: root inspected all eight frames, 98 checks, 960/1280; notice remains outside scroll content and all actions fit; `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-4782a007421b4d25a1de70a69f48bf1a/checkpoint_notice_frames`.
- All three new behavioral fixtures are registered in the full runner. The low-level fixtures inject failures at file-operation boundaries in disposable profiles; rendered UI states are staged. They do not simulate physical hardware power failure.
- Full-suite integration exposed two old independent-run fixtures relying on silent deletion of their deliberately partial doorway records (`test_room_layout_entry` and `test_brittle_cover`). Both now clear those isolated codec records before starting the next fresh run. Scoped compilation/guards and both corrected fixtures pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-4dd2015e4eac44adb664a9b402b5fda7`. The first stalled fixture and interrupted rerun were stopped only by their verified temporary project/process identities; no player process was touched. A bounded regression-process timeout is a concrete follow-on pipeline fix.

Second checkpoint is committed and pushed as `5a0f1ef856d422d948af9746b528ea202f9cd07a` (`Protect Continue checkpoints and handle storage failures`). Its full hook passed **314 scripts and 76 gameplay fixtures**, with logs in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-81f5616a55c44b0f8990ea7703b4b2bb`. [Hosted CI passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34445668356).

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-continue-candidate-7ea873f9dd4c4c8c9382cea2a13a57d9/AbyssalDescent.exe`.
- Build ID `dev-20260910-061914845-5ffe01a0`, SHA256 `306820628C2D97C7CD04AC72C7B265DB95276C055C74296604623226679E589D`, 114252848 bytes.
- Native candidate validation passed 27 package and 62 actual-executable checks, with isolated services/profile and external driver as documented above; `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-5723d489a3ef4882b0ad5413d5ea674a/report.json`.
- Source identity compares 524 production files; 522 match exactly and the two expected build-ID/debug-disabled overrides were independently inspected. Saved in candidate `source-identity.json`. Later fixture-only fixes do not change this production package. This candidate remains separate from the desktop until final workday delivery.

## Third milestone — Reward handoffs and validation

Two ordinary gameplay defects were reproduced using shipped powers/bonuses in disposable data. Fix these existing behaviors without changing reward odds, bonus strengths, movement tuning or combat wording:

- **Mission bonus refresh:** Fortified → room clear → Overcharge → room clear → Fortified again removes the refreshed Fortified entry when another bonus follows it, dropping damage resistance from 15% to zero. The cleanup loop incorrectly deletes the replacement. Preserve one refreshed entry and the unrelated bonus's remaining duration; verify actual effects and first/middle/last positions, plus existing stack/replace behavior. `buildcraft_opportunities` owns `player.apply_objective_mutator` and focused regression coverage. Original reproduction: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-d349d0e41561426db2d74e0512cead54/mutator-audit.log`.
- **Motion completion across room clear:** actual Cut the Signal with Razor Orbit and Sovereign Tempo lets an Orbit-completion wave kill the marked target and open rewards. The calling transition then enters Carry, or spends a Blast charge and emits an Attack after combat is cancelled. Guard the existing completion callbacks using cancellation generation and eligibility; revalidate Orbit targets afterward. A cleared room must leave motion None and preserve the unspent Blast charge; ordinary Orbit release and Blast still work. `encounter_opportunities` owns the motion controller and focused actual-world tests. Original reproduction: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-d9ef6d6e615544fab0786d9178f1e98e/motion-audit.log`.

One supporting validation increment: `pipeline_audit` owns reusable full gameplay validation before release export/publish and bounded regression-process execution. The release caller must use the exact tagged commit, read-only validation, success-only dependencies and caller-aware concurrency; keep release assets and existing gameplay check names. Add a timeout that retires the exact owned process tree (Godot's console wrapper launches a child), retains evidence and restores isolated environment values. Verify PowerShell 5.1, inert success/error/timeout cases and actionlint; normal branch CI is the hosted evidence. Do not create a release tag or publish a test release to test this change. Tags pointing to older commits still use their older workflow definitions.

Root owns documentation, central fixture registration if needed, independent review, package verification and delivery. Continue on the existing branch; do not merge or tag it. Stop new features at 13:30 local and finish by 15:00 as above.

Third-milestone focused evidence so far:

- Mission refresh: 124 checks plus 895 existing power/snapshot checks, all 315 scripts and guards pass; `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-ad65ff49221345329e6083b9f47fbef1`. Independent review found no issue. Tests cover all six authored bonuses in three list positions, legacy matching, duplicate collapse, cap/policies and actual effects: Fortified's 15% resistance, Overcharge's 20% shorter Attack/Dash cooldowns (no damage bonus) and Hunter's Focus's 25% damage bonus. Its new fixture is registered in the full runner and UID collected.
- Motion completion: 302 checks with all 315 scripts and guards pass; `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-899e439812144ee8aebdf54b290ee7b8/test_arcana_motion-console.log`. Includes callback cancellation with unchanged eligibility, eligibility loss alone, replacement motion, invalidated next anchors, and actual Main objective clear through the release tick plus same-frame movement. Existing charge, movement, contact and provenance cases remain. No new network payloads were introduced; this evidence is not internet co-op acceptance.
- Independent review reproduced one narrower continuation in the actual Player physics frame: after motion was cancelled, ground movement still used the direction captured before rewards, moving 0.467px and setting nonzero velocity. The frame now stops before further input or movement when a callback ends combat, freezes input or kills the actor. Final focused motion evidence passes **304 checks** and all 315 scripts/guards in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-1d55666a06f840f9846c334aabdc0a50`. Both real-Main release scenarios execute the whole Player frame with held movement and end at the exact original position, zero velocity, no additional Attack and no Blast charge spent. Ordinary noncombat movement and survey suppression also pass.
- Process helper: **57 inert native checks on both Windows PowerShell 5.1 and PowerShell 7**, in `C:/Users/mikel/AppData/Local/Temp/abyssal-process-tests-c3b68ed64a884ccba0c24faf547e0b5f` and `C:/Users/mikel/AppData/Local/Temp/abyssal-process-tests-4dc313a6f7df4a689927211bf8b3f8ec`. It retains the process handle to read fast exits correctly, quotes Windows arguments, checks both streams including a final error before a zero exit, times out wrapper/child trees, retains logs and restores the runner's isolated environment variables. Sandbox restrictions denied tree termination during an earlier test, which correctly reported cleanup failure; scoped elevated cleanup verified those inert PIDs retired, and the permitted run proves normal Windows tree cleanup. Real Godot under PowerShell 5.1 passes all 315 scripts/guards and 159 checkpoint lifecycle checks with no owned process left behind: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-f411a305881649608ae6ab205561286d`.
- Both workflow edits pass cached actionlint 1.7.12 with optional ShellCheck/Pyflakes disabled. CI runs the inert process checks before its full gameplay suite and retains their logs. Full checkpoint checks and hosted CI remain required for this milestone.

Independent final motion review is clear. The normal third-milestone candidate is `C:/Users/mikel/AppData/Local/Temp/abyssal-handoff-candidate-a7063b21886f49c5ad24224182318c1e/AbyssalDescent.exe`, build `dev-20260910-064513198-2d092f2b`, SHA256 `CE84FA189CC1457F3841CE6A024B37CD7365C21A6A71823350A5BCEC7C3187FB`, 114253456 bytes. It passed 27 package and 62 native checks with no shutdown warnings; `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-4caed5219148410fbab6a01df5b30199/report.json`. Its `source-identity.json` verifies 522 of 524 production files exactly, plus the independently inspected build-ID and debug-disabled overrides. Desktop delivery remains deferred to the workday's final build.

The first full third-milestone hook stopped on the cover Orbit fixture (`C:/Users/mikel/AppData/Local/Temp/abyssal-validation-55420c77e984472883453bb6ba579ad4`). The new legitimate anchor validation exposed old fixture setup: several same-frame room replacements left 31 pending column bodies in the sight query. After physics/process retirement there were four, sight was clear and Orbit entered normally. The fixture now waits and asserts a live Orbit before breaking its anchor. The separate ENet fixture now grants the actual L2 terrain unlock instead of calling terrain Orbit directly at L1. No runtime behavior was weakened. Corrected focused evidence passes 100 cover and 304 motion checks plus all 315 scripts/guards in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-b0ef3c284ac9450dbcf4151d34c11fd2`; corrected ENet passes 90 host/93 client checks in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-620b403b23244bf9b86b3d4a1bc04503`. These fixture-only changes do not alter the verified normal candidate.

Third checkpoint is committed and pushed as `75f16833d0d88a19a8bc8d0fb495bcb90521620c` (`Fix Mission refresh and reward transition callbacks`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-c4e66b59a9a5472881376eff6309e9a4`. [Hosted CI passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34447488142): 57 inert process checks, all 315 scripts and the exact 77 expected fixtures. Independent artifact inspection found 395 log files and no non-log files, including real hosted parent/child timeout cleanup evidence. Retained local proof: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-75f1683-b1676c3905c4488881aab037e7984445`.

## Fourth milestone — Mouse reward comparison

Native pointer dispatch reproduced a small reward-inspection defect: point at the third offer, move to Your Build and click it, and the modal compares the first offer because leaving the cards clears hover. Preserve a separate last-pointed/focused comparison index, while leaving the live hover index at -1 outside cards so footer clicks cannot confirm a remembered reward. A current valid hover takes priority. Reset the comparison on new offers, successful rerolls and closing; no reward content, odds, powers or confirmation rules change.

`buildcraft_opportunities` prepared this in a disposable copy during the third checkpoint freeze. Root reviewed and copied only the reward UI and its existing fixture after the checkpoint committed. Focused evidence passes all 315 scripts/guards, **490 reward/build inspection checks** and the existing reward-input fixture in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-72a1bc9de45042d899df1cc6e63a88be`. Native pointer tests cover every nonfirst card in three/four-card layouts, Return, keyboard-to-pointer use, Reroll, Skip, replacing an open offer, and unchanged offers/RNG/reroll counts. The existing Mission skip assertion now compares its own event-count delta so the earlier pointer case can also exercise Skip. Root verified both copied files match the tested hashes exactly.

Fourth checkpoint is committed and pushed as `0131710384fab43d0328fca0cd9cbd653241ee57` (`Preserve the pointed reward for build inspection`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-129526ce4f35421aa961869c24985369`. [Hosted CI passed for this exact commit](https://github.com/Excellund/AbyssalDescent/actions/runs/34447977064). Independent downloaded-log verification confirms 57 inert checks, 315 scripts, all 77 expected fixtures, 490 reward/build inspection checks and no unexpected gameplay errors. The artifact contains 395 log files only, with seven-day retention; local evidence is `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-0131710-ab81f2ae0beb47f7ad19d39ce5d1254b`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-comparison-candidate-9b9f065ff27c497ea4e764a705ac6f52/AbyssalDescent.exe`.
- Build `dev-20260910-065750002-5b3e79ef`, SHA256 `0C7AC8191401B5BDE45763332AC72B106850F723F9855F62E138EA69A8E5B33A`, 114253664 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-0ee861756d9743c2a937cbcf315c42d7/report.json`.
- Source identity: 522 of 524 production files exact, with only independently checked build-ID and debug-disabled overrides; candidate `source-identity.json`. The desktop build remains the accepted prior playtest pending final workday delivery.
- The first export correctly rejected a zero-exit audio-import Unicode parsing error, logged in `C:/Users/mikel/AppData/Local/Temp/abyssal-playtest-export-48d90177e4834ae0a31287fc9c58039d/import-console.log`. One fresh isolated retry passed with unchanged source/assets; its staging is `C:/Users/mikel/AppData/Local/Temp/abyssal-playtest-export-506b031003fc44139b48c2e87260c074`. No error was ignored and no audio or exporter code changed. Cause remains unconfirmed; investigate if it recurs.

## Fifth milestone — Accurate survey hints

The existing objective sidebar already gives useful guidance; extra banners or widgets are unnecessary. An isolated actual-Main audit instead found misleading live-status wording before simulation begins: Intercept says "Path clear — drone advancing" while survey is active, progress is zero and the player is 379px away from the 240px escort radius. After Engage the same full world frame correctly changes it to "Stay close to the drone". Hold the Line says "Re-enter the zone before decay" during survey even with the player 1px from the zone center, because its initial inside flag has not been ticked. Circuit Sweep's existing survey instruction is already accurate.

Use concise, neutral instructions in the existing affected objective-card lines while `encounter_intro_grace_active` is true; retain live advancing, blocking, contested and return messages after Engage. Preserve act/biome banners, readiness input and co-op readiness, timers, geometry, rewards and all mechanics. Verify the actual room-entry HUD before/after Engage and reuse across rooms, including readable 960/1280 presentation; no new networking payload is needed for this local display condition. Scope belongs in `world_hud.gd` and relevant existing isolated fixtures, with independent review before the next checkpoint.

Implementation is frozen for its checkpoint: root changed only the two affected HUD branches, using "Hold the zone uncontested" and "Stay near the drone; clear its path" during survey. `encounter_opportunities` owns the existing room-entry fixture's real Main state checks; `buildcraft_opportunities` owns temporary GPU captures and fit verification; root reviews the wording/mechanics, rendered frames, documentation and delivery. The native control rule advances progress while a player is inside an uncontested zone; Intercept advances only with a nearby player and no enemies in its blocking radius. The new instructions preserve those conditions without claiming a new failure rule.

Focused verification passes all 315 scripts/guards and **90 room-entry checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-8ee93acdc3ba4b03a021a8d2074be38a`. The existing fixture drives actual World frames and 3.2 seconds of UI time for Hold, Intercept and Circuit; the serialized objective state remains frozen and the card survives the entry banner's fade. After Engage, real player/enemy positions produce the existing stable/return/contested and outside/advancing/blocked messages. The next ordinary room clears the card. Existing objective serialization produces the same survey display on a staged replica; no new socket or RPC behavior is claimed. Root's independent source review found no issue.

GPU verification passes **65 checks across eight frames**: Hold and Intercept before/after normal movement Engage at 960/1280. Both full survey hints fit the existing 302px card (183px and 230px), as do the returning live messages. Act, biome, Bearing and survey readiness presentation remain correct. Owner inspected every final frame; root inspected all eight states across the equivalent initial/final renders. Final manifest and captures: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-f42e873d22f74e0494d123fa40766edc/survey_hud_frames`. Source/preparation/validation HUD hashes match. The temporary fixture stages enemy positions and freezes actors; it does not alter production gameplay or the player's profile. Full checkpoint, hosted CI and normal candidate verification remain.

Audit evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-b0ef3c284ac9450dbcf4151d34c11fd2/survey-card-confirmed-console.log` (exit zero). An earlier scratch probe's label typo caused a timeout; its verified temporary wrapper PID19444 and descendants were retired through scoped approved cleanup, and the corrected probes exited normally. No production files were changed by that audit.

Fifth checkpoint is committed and pushed as `6a4ec7fc5e9b7d3df844cd20468203b0db29a643` (`Clarify objective hints before combat starts`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-dc81a49ed1a8499dac2e4d6693f02d67`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34449991365): 57 watchdog checks, 315 scripts, all 77 expected fixtures and 90 room-entry checks. Independent downloaded-log review found no unexpected gameplay errors; the retained artifact contains 395 log files only, expiring September 17. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-6a4ec7f-33406a69abfe4c50b5bf16f268043500`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-survey-candidate-045ad4427035468d83a30b1033a99f49/AbyssalDescent.exe`.
- Build `dev-20260910-072120711-19796323`, SHA256 `0E7B5DEEC78848C442660E57BE53A441628E00FB99B3C6F6708681EC0244DB9A`, 114253664 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-4a6e36a8256c47528c4eb6a8ba93ffe5/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. Export/import passed on the first attempt. The desktop hash still matches the accepted previous build; final workday delivery remains pending.

## Sixth milestone — Show effective action cooldowns

An actual Main probe found that Overcharge's existing 20% cooldown benefit is absent from the Stats panel. Veilstrider's accepted Attack/Dash intervals change from 0.24/0.30s to 0.192/0.24s, but the HUD continues showing the raw 0.24/0.30s values. They look identical before, during and after the earned bonus. Reproduction exits cleanly in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-8ee93acdc3ba4b03a021a8d2074be38a/audit_overcharge_hud-console.log`.

Expose the current ordinary Attack/Dash interval calculations as Player accessors and use them at the existing action assignments and the two HUD reads. Keep the exact Overcharge multiplier, existing floors, base properties and serialization, attack/Blast timing, and Veilstep's empowered zero-cooldown exception and refunds. Stats describes the ordinary interval, not the current remaining timer or a promised special activation. Do not change labels, bonus-row layout, Boon previews, bonus strength or power content. Verify accepted actions against actual HUD values through earning, refresh, expiry and snapshot restore, plus minimum intervals and the Veilstep exception.

`buildcraft_opportunities` prepares Player/HUD and the existing Mission fixture in a disposable copy; root reviews and integrates after the fifth checkpoint, owns documentation and delivery. A separate audit confirmed that a fallen co-op avatar can still escort Intercept because its proximity loop omits the living-player filter already used by Hold/Circuit. `encounter_opportunities` may prepare that narrow follow-on in a separate disposable copy, but it remains queued until this cooldown milestone is complete. Neither audit changes the shared source during a checkpoint freeze.

Sixth-milestone implementation is reviewed, integrated and frozen. Root copied exactly the three files validated in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-2ae725c97bcb41a9a9ae735ef9b04847`: all **315 scripts**, both guards, **215 Mission/Stats checks**, **895 snapshot checks** and **304 motion checks** pass. The same formulas are used at all three prior assignments without moving them across callbacks. The existing Mission fixture covers all four characters, accepted Attack/Dash/Blast, refreshing without changing running timers, real disk resume and repeated restore, expiry, existing 0.05/0 floors, shared refunds and Veilstep's empowered free Dash. No fixture registration or new schema is needed.

Root inspected all six GPU captures at 960/1280: baseline Stats 0.24/0.30s, earned Overcharge 0.19/0.24s, and expiry back to 0.24/0.30s. **32 checks pass**, with the existing Stats panel fitting fully at both sizes and raw cooldown properties unchanged. Actual Main/Mission handoff is used, with room/actors staged and frozen; these are display checks rather than normal-progression balance evidence. Captures and manifest: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-82cfe10147ef423c91a3cb3ddd19ef03/cooldown_hud_frames`. Final rendered Player/HUD hashes match the integrated and scoped-validated production files. An earlier temporary capture fixture used an incorrect preload path and was rejected before running; only that scratch path was corrected. Full checkpoint, hosted CI and normal candidate remain.

Sixth checkpoint is committed and pushed as `93f8036483fb02ab5af8d9c74885c0f3eea667b5` (`Show effective action cooldowns in Stats`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-a8570294be2145a8a840a7b2e95e7ea9`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34450978279): 57 watchdog checks, 315 scripts, all 77 expected fixtures and 215 Mission/Stats checks, with no unexpected gameplay errors. Its artifact contains 395 log files only, retained until September 17; downloaded verification is in `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-93f8036-529cde62b4574bdbbb0707e8d5ec12e5`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-cooldown-candidate-e5c3a379e6484061bac6d31ac3e3b448/AbyssalDescent.exe`.
- Build `dev-20260910-073237340-565fa15e`, SHA256 `6B78CDCB3B7C93038CC5CA90B20F20E0F9EB574A0D35F789D8FC12442F829A6C`, 114254016 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-3bed896f116e473bb9e64df92e4fc679/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. The normal desktop replacement remains deferred until final workday delivery.

## Seventh milestone — Living Intercept escorts

An actual two-avatar Main probe reproduced fallen-host and fallen-remote avatars advancing the drone while the sole living teammate was 580px away from its 240px escort radius. Native death handling hides the fallen avatar and leaves the encounter running, but Intercept's raw proximity loop omits the living-player condition already used by Hold and Circuit. Reuse that existing helper, preserving the same roster/multiplayer guard, inclusive radius, authority and all spawning, timing and blocking rules. Either living teammate may escort; fallen avatars cannot. No new save or network fields are needed.

`encounter_opportunities` owns the narrow runtime delta and extensions to the existing room-entry native/ENet fixtures. Root and `buildcraft_opportunities` independently reviewed the helper reuse and native fixture with no actionable findings; root reviewed the ENet extension as well. The native fixture exercises actual health/death signals and World frames for both roles, live HUD/codec parity, living escort resumption and enemy blocking. Original logic gives 104 checks with six expected failures; the proposed logic gives **104/104**. Evidence before/after is retained in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-8ee93acdc3ba4b03a021a8d2074be38a/escort-baseline-verified-console.log` and `escort-after-final-console.log`.

Initial connected-peer evidence passes **218 host and 214 client checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-5ebff9cd4c154ae69cc80d5d870d20a5`. Real health/alive RPCs and native World death handling hide either fallen avatar, the host alone advances the objective, and existing objective RPCs deliver matching stalled/progress/escort/HUD state. A living teammate resumes, an enemy blocks and production host revival reaches both peers. Room offers and actor/enemy positions are explicitly staged; this is not internet lobby or movement-input acceptance. A fresh normal-source preparation now includes the sixth checkpoint's Player/HUD accessors, and relevant native/ENet checks are being repeated against that combined candidate before integration.

The combined candidate is reviewed, integrated and frozen. Standard isolated validation compiles **315 scripts**, passes both guards and **104 room-entry checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-15f8ff30aed6430baf4c7ee7b3e28a94`. Final real ENet passes **218 host/214 client checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-f14c11e105ec47cdb678ee01b24ad41f`, including the existing complete descent, Rest, bosses and final summary. A rerun exposed a fixture race: consecutive Intercept rooms share a label, so label-only waiting could accept the prior room. The existing helper now also requires the combat room ID to advance for encounters/bosses; Rest retains its existing behavior because it does not advance that ID. Production eligibility remained the same one-line helper replacement throughout.

Root verified and copied only `objective_runtime.gd`, the existing room-entry fixture and its existing ENet fixture from the final normal-source preparation. The final ENet-only waiting correction compiled and ran after the native scope. Production/native fixture bytes match across preparation/native/ENet, including the sixth checkpoint's Player/HUD/Stats fixture; final ENet fixture bytes match preparation and final ENet. Exact matrix: `C:/Users/mikel/AppData/Local/Temp/abyssal-escort-source-7c044e80aa914c8da435259f49ec3a37/escort-validation-manifest.json`. Full checkpoint, hosted CI and normal candidate remain.

Seventh checkpoint is committed and pushed as `7e20ff5cc6b30341487e4f2ef0f5691182b56a3c` (`Require a living player to escort Intercept drones`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-efa3efac6d4746b981743985668fce0c`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34451908998): 57 watchdog checks, 315 scripts, all 77 expected fixtures and 104 room-entry checks, with no unexpected gameplay errors. Its artifact contains 395 log files only, retained until September 17. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-7e20ff5-70c8e8ea49494861bddd390ab81053f3`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-escort-candidate-7ed710a34ea245e69f319b7a9c0ce4ab/AbyssalDescent.exe`.
- Build `dev-20260910-074334874-f63dddba`, SHA256 `8AF3927B2C2DF9737A8AA0C1E99F100FA752951AAC643A7D9398BB1BB59A70A7`, 114253808 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-9e63e61f8c1e4446a63680027416c14c/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. Final normal desktop replacement remains pending.

## Supporting cleanup — Correct join log severity

Read-only audit found ten unconditional success/progress `push_error` calls: four in the native session manager's connected callback and six across Menu join progress/waiting/success. Each already has an adjacent normal print or existing diagnostic log. Remove only these duplicate error calls so ordinary progress does not emit engine ERROR entries. Preserve true empty-session, connection-failed and service/resource failure reporting, along with all control flow, timing, callbacks, signal cleanup and useful diagnostics.

`pipeline_audit` prepared the ten-line deletion in a disposable copy; root independently reviewed and integrated it after checkpoint seven. No new test or connection infrastructure is needed for this logging-only change. Existing ENet fixtures suppress autoload startup and manually establish connected state, so their green results do not exercise the native connected callback or full Menu join waiter. The audit therefore establishes the severity problem from the unconditional source calls, not from a new end-to-end internet/Menu probe. Required full checkpoint and normal candidate checks will verify the integrated source; complete internet join acceptance remains an existing human-playtest question.

Eighth checkpoint is committed and pushed as `74daa9972d91074b7f4d3801cacd15aa2ebcea74` (`Stop reporting multiplayer join progress as errors`). Its full hook passed **315 scripts and 77 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-71238b3cac994f02a9c779c7e25c7eb3`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34452645134): 57 watchdog checks, 315 scripts, all 77 expected fixtures, 104 room-entry and 215 Mission checks, with no unexpected gameplay errors. Its artifact contains 395 log files only, retained until September 17. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-74daa99-871f11400cd347f3957525f41d3cc9ee`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-join-log-candidate-e302334acaf246f79f39b37eb3c8c3a1/AbyssalDescent.exe`.
- Build `dev-20260910-075112740-386a7507`, SHA256 `10E0AC0F196398F59FA66F36F8A9E3E312DD38C7313AD7821B440388C241D08B`, 114253600 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-c6fd855a9095439b8607b9fdab1a6d96/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. This remains a temporary candidate; final normal desktop replacement is still due before 15:00.

## Ninth milestone — Cancel pending joins when leaving the menu

A bounded native loopback audit confirmed a player-visible cancellation defect. Pressing Escape/Back while joining shows the root menu and hides the multiplayer panel, but retains both waiter listeners and the pending transport. A later real ENet `connected_to_server` event runs the production manager callback and makes the actual Menu waiter return `{ok: true}` with an active session. The normal `_join_multiplayer_room` caller then has an unconditional success route to the lobby; the audit stopped before lobby/public-service startup. Normal success and short timeout listener cleanup passed separately. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-f6e811ebc977438285d65ea8ce211d05` (host 5/client 10 checks; no unexpected errors). It uses two actual loopback peers and a minimal Menu startup override with real panels/input/waiter/manager callbacks, isolated profiles and no registry/tunnel/UPnP services.

Cancel the owned join when Back/Escape returns to the root menu or Menu tears down, close that attempt's peer/join timer and retire its listeners. Guard asynchronous resolve, probe, retry/backoff and waiter continuations so a canceled attempt cannot start a connection, retry, open the lobby, overwrite later UI or clean up a newer successful join. A fresh join after cancellation must work. Preserve ordinary success/failure/retry behavior, accepted lobby sessions and existing duo autostart; avoid a host/registry/transport redesign. Delayed local service fixtures may verify pending phases, but no unattended public service or player-profile use is permitted.

`buildcraft_opportunities` owns Menu implementation, a narrow local session-manager operation identity and focused lifecycle coverage in a fresh disposable source. `encounter_opportunities` owns independent review and real connected cancellation/success evidence using the local audit harness, coordinating the attempt interface with its owner. Root owns documentation, final source review/transfer, central fixture registration, full checks and delivery. `pipeline_audit` independently reviews cancellation ownership and late continuations. The ninth implementation is now reviewed, integrated and frozen for its checkpoint.

Review found that a single manager join can replace its peer during address fallback, or clear it after terminal timeout while retaining session metadata. An exact initial-peer check therefore misses owned cleanup. Track a local operation generation that survives fallback/connection and changes on replacement, leave or host takeover; cleanup must match that operation before touching its current peer or metadata. Rejected starts must not claim an unrelated existing session. This identity is local bookkeeping and adds no RPC, save or protocol field.

- Focused lifecycle fixture: **75 assertions across 13 cases**, all **317 scripts** and both guards pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-b26eb40513e94d0d9debf48e64e491f2`. Delayed resolver/probe completions, previous-session cleanup, waiter/backoff/reprobe, terminal four-attempt failure, same-registration and reused-peer replacement, native HTTP cancellation, Menu removal and duo polling/warmup/retry/success are covered. Local resolver/start seams stage asynchronous phases; the native HTTP case uses an owned TCP loopback listener that never responds. Existing checkpoint Menu 68, panel/focus 183 and helper-ownership 37 checks also pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-726bb013a4014d4898dd181cae24d7d7`.
- Actual loopback ENet: **12 host/37 client checks** pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-98942bf3717940e79d16bb40fbfd6e43`. Production connection callbacks and Menu caller/waiter handle normal success, actual Back/Escape, same-frame same-registration replacement, address fallback and accepted-session navigation/teardown. The two injected timeout cases are recorded separately; one existing failure diagnostic is suppressed only during its synchronous injected callback, checked against its exact expected reason and printed as an expected failure. No generic error filtering was added; there are zero unexpected errors.
- Discovery is a local resolver stub, autoload startup is suppressed and the lobby-opening boundary is observed without starting lobby protocols. These results do not establish public registry, tunnel, UPnP, full lobby or internet co-op acceptance.
- Root and both independent reviewers found no remaining blocker. Root verified hashes and copied only Menu, session manager, the two new fixtures and UIDs, and the thin ENet helper. The headless fixture is registered in the required runner; ENet remains a scoped check. Production hashes match both focused and connected evidence; matrix: `C:/Users/mikel/AppData/Local/Temp/abyssal-join-cancel-prep-fad97277dd394c95b3ac6a88a1c74d3a/join-cancel-enet-manifest.json`. Useful diagnostics and true failure reporting from checkpoint eight remain intact.

Root also ran the existing read-only local telemetry analyzer for the accepted desktop build `dev-20260909-214203135-ea25d8ac`, September 9 00:00 UTC through September 10 08:00 UTC. It found one completed non-debug solo Hexweaver clear to depth 24, with room/damage/reward events and recorded provenance. Report: `C:/Users/mikel/AppData/Local/Temp/abyssal-balance-20260910-081342-40ab32d3.json`; the source save's SHA256 was unchanged. This single run provides no basis for numerical balance changes or conclusions about unused powers; no new gameplay scope was selected from its aggregate metrics.

Ninth checkpoint is committed and pushed as `20965dbe4167352f70dee26debefed8905373eb8` (`Cancel pending multiplayer joins when leaving the menu`). Its full hook passed **317 scripts and all 78 gameplay fixtures**, including 75 join-cancellation assertions, in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-165adf6efa49426cbb3dcae63d84f557`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34455093015): 57 watchdog checks, 317 scripts, all 78 exact committed fixtures and 75 join-cancellation assertions, with zero unexpected gameplay errors. Artifact contains 399 log files only, retained until September 17 08:31 UTC. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-20965db-9891f56cb092400386b9e0d19d8800fd`. Automatic review initially rejected the push because it considered the destination unverified; read-only checks confirmed the existing public `Excellund/AbyssalDescent` origin, ADMIN access and expected accepted-main/prior-workday remote commits. The same non-force push then passed review; nothing remains blocked.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-join-cancel-candidate-34411eb6e42848ebb1bd3d4f13c04ff5/AbyssalDescent.exe`.
- Build `dev-20260910-081851539-f2a0c425`, SHA256 `1E84ECB021064FAC09489DF2177A3019C08C5F4EFB9B09A3D9BD93FE6DDFEC0B`, 114256976 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-ac9608f158e54ca787e8254a08900b43/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. Final normal desktop replacement remains due before 15:00.

## Tenth milestone — Accurate Run History outcomes

An actual Pause → Abandon Descent → recorder/store → History probe reproduced a saved `abandon` result displayed as “Defeat — Bastion.” The panel treats every non-clear result as a death in its list and detail. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-b26eb40513e94d0d9debf48e64e491f2/history-abandon-audit.json` and its console log; four setup/flow checks pass and the incorrect label is observed.

Use one presentation mapping so History distinguishes Abandoned and the existing `host_left`, `menu_exit` and `quit` outcomes. Preserve exact Victory/Defeat presentation, show a neutral “Run ended” for unknown/missing outcomes and make neutral rows recognizable. Keep saved outcomes, context/progression semantics, statistics and clear rates unchanged. Inspect list/detail at 960 and 1280, including the longest character/co-op text. No broader History redesign is needed.

`buildcraft_opportunities` owns the panel and extensions to the existing result-identity and checkpoint-lifecycle fixtures in a fresh disposable copy. `encounter_opportunities` owns independent review and actual GPU captures there. Root owns final source review/transfer, documentation and checkpoint/delivery; `pipeline_audit` verifies ninth-checkpoint hosted CI. The tenth implementation is reviewed, integrated and frozen for its checkpoint.

- One shared presentation mapping supplies the list marker/color and detail heading. Clear/death retain their exact existing 58px rows and styling. Other outcomes use a neutral marker/color and explicit third status line in a 78px row: Abandoned, Host disconnected, Returned to menu, Exited game or Run ended. No saved data or progression behavior changes.
- Focused validation passed **317 scripts**, both guards, **127 result-identity checks and 175 checkpoint-lifecycle checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-a56b041fda034e52b8b4b8117bfde9d5`. The actual Abandon button and World death/final-boss-clear handlers persist through recorder/JSON into History; Abandon retains its existing `death` progression context independently of its `abandon` record. Other known, unknown, empty and missing outcomes are staged through the real history store. Selecting/reopening History preserves saved bytes.
- Rendered review passed **16 frames and 315 checks**, including clean audio retirement, at 960/1280 on RTX 4080. All outcomes, longest character/four-player text, third-line bounds, headings, scrolling and Back fit. Independent reviewer inspected all 16 frames against the same production hash; root inspected four representative views and two final recaptures. Final manifest and console agree after the fixture was adjusted to include teardown results: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-65c9c5ac3c2c4cc7b656fb0438d6b393/run_history_frames/manifest.json`.
- Root verified source/candidate hashes and copied only the panel, two existing fixture extensions and new render fixture/UID. No new headless registration is needed. The new render fixture uses the existing generic rendering helper; production does not include test files. Hashes and review record: `C:/Users/mikel/AppData/Local/Temp/abyssal-history-outcome-prep-2f827c9649694eff8fd5c75d83424d5e/history-outcome-render-report.json` and `history-outcome-hashes.json`.

Tenth checkpoint is committed and pushed as `15ebd28ae0987c190400688209f2bddc4b2b9d41` (`Distinguish abandoned and interrupted runs in history`). Its full hook passed **318 scripts and all 78 gameplay fixtures**, including 127 result-identity and 175 checkpoint-lifecycle checks, in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-eed764be7918407eabc612106d01e63b`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34456179289): 57 watchdog checks, 318 scripts, all 78 exact committed fixtures, 127 result-identity, 175 checkpoint-lifecycle and 75 join-cancellation assertions. There are zero unexpected gameplay errors. Artifact contains 399 log files only, retained until September 17 08:43 UTC. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-15ebd28-52bd0e12e8314eee9d9afe6c43a8b47e`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-history-candidate-0be763d27855401da2836b5a4db30d8d/AbyssalDescent.exe`.
- Build `dev-20260910-083201315-9e0e233d`, SHA256 `159F813CBCD5F3F08E97B6ABDA8FC658676D0D0C590A21CD487E24640A75CA6C`, 114257696 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-89534f3707d248e9b1efc51549cd9d4a/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. The desktop still contains the accepted prior build. Final normal replacement remains due before 15:00; the continuation stays active for further bounded work and delivery.

## Checkpoint-ten batch handoff

All ten implementations are committed, pushed and verified locally and on hosted CI. No implementation, review, export or test process remains active. Only post-checkpoint updates to this workday record and the development-plan overview remain uncommitted; preserve them for the next checkpoint. The next batch should choose a fresh concrete finding, not repeat completed implementation or unchanged checks. The existing 20-minute continuation remains ACTIVE until 15:00 Copenhagen, with new features stopping at 13:30. Final normal desktop delivery and its package/hash verification are still required.

At 10:38 local, the desktop SHA256 remained `C1DC83BFA6BED7AA73A7C474606ECCF2D7D02BCE29EAF64426CCB62CC5C82288`, the accepted prior build. `main` and `v0.6.3` still point to `922a4b576165f6af8477f69bc4c41a48f64916b1`; the current workday upstream matches `15ebd28`. The initial ninth push-review rejection was resolved through destination/authorization verification; no approval or external blocker remains.

## Eleventh milestone — Preserve co-op revival across death replication

The next bounded Rest/reward audit found no justified new defect: existing flow and fixtures cover local healing/caps, post-heal checkpoints, reroll consumption/reveal limits and confirmation/skip input handoff. A separate co-op recovery probe reproduced a stronger problem. The host sets the joining player's health to zero and kills the final enemy in the same turn; normal encounter clear revives that player to 1 HP. The joining client's ordinary death callback echoes dead status back afterward. The host then sees a 1 HP player who is invisible and removed from combat, while the joining owner remains visible. Native reward completion, door transition and following-room Engage do not repair the host's removed/hidden state. A later snapshot can restore the internal alive flag, so the durable defect is the visibility/removal disagreement rather than a claim that every liveness flag stays false.

Reproduction uses the existing two-process isolated Main harness, native health/death/revive RPCs, actual reward skip and door/readiness flow. Actor damage and last-enemy death are deliberately staged in one host turn; no forged network packet or production edit creates the race. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-a676a05f59f74050b40a09942719c234` and `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-93e31485c1d84755851afdfc006c96b7`. Initial assertions expected `is_dead()` to stay true, but that method reads health; the logs instead identified the exact lasting hidden/removed mismatch. The dedicated regression will assert that state directly.

Match death/revive replication to the existing host-authoritative HP policy: only the connected host emits those life updates, and receivers accept host-originated life state. Preserve RPC argument signatures, health sequences, local death/audio/stat handlers, 1 HP revival and stored data. Do not change the shared owner-authority helper used by movement, actions and cues. Verify the ordinary race, repeated down/revive, normal host/remote deaths, next-room presence/collision/eligibility, and rejection of non-host life updates without weakening legitimate owned actions.

`buildcraft_opportunities` owns the narrow PlayerReplicationService change and a focused headless authority fixture in a fresh disposable source. `encounter_opportunities` owns the native reproduction and dedicated real ENet fixture/helper. `pipeline_audit` independently reviews authority and ordering. Root owns scope, documentation, source review/transfer, central registration and checkpoint/delivery. The eleventh implementation is reviewed, integrated and frozen for its checkpoint. Readiness-count feedback remains an unselected observation; no readiness protocol change is part of this slice.

- Death/revival broadcasts now require the connected host. Both life receivers reject non-host senders through a life-only helper; direct calls outside RPCs use the manager's current session role. Native Godot `call_local` already reports the actual peer ID, so this direct-call fallback addresses stale cached role bookkeeping rather than a native RPC sender defect. Shared movement/action authority and the existing HP resolver remain unchanged.
- Focused isolated validation passed **320 scripts**, both guards, **65 life-authority, 64 shared-status lifecycle and 11 player-feedback checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-5685537ec24444778266b41e528efeaa`. Repeated host/remote-avatar death and revival, current/stale cached roles, native host `call_local`, client broadcast suppression, owner cues and solo/no-session behavior are covered. The fixture explicitly invokes the custom service's broadcast boundary; automatic Player-to-autoload routing is established by the connected fixture. The final source differs from the focused snapshot only by a corrected explanatory comment; the full hook will check the final bytes.
- Final real ENet passes **131 host/125 client checks**, zero failures and zero unexpected errors, in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-f7a23e31b43548c48785eef38468591d`. The same final fixture on the preceding source reproduces six expected host failures and one client failure in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-9d9776beb332440498e41f99a8f7906b`. Five host failures establish the natural same-frame race and its persistence into the next room; separate non-host message tests account for the remaining two. Those replay tests run after natural next-room/movement checks and cannot repair the original race before it is asserted.
- Actual death/clear/revive callbacks cover same-frame joiner fall, separate joiner fall and separate host fall, exact 0/1 HP, visibility/removal, real colliders, targeting eligibility, reward skip, next-door/Engage and owner input/transform propagation. Actors and the encounter are staged with local peers; this does not establish public registry, full lobby or internet acceptance. No diagnostic filtering was added.
- Root and independent reviewers found no remaining blocker. Root verified and copied the service, both fixtures/UIDs and thin ENet helper from `C:/Users/mikel/AppData/Local/Temp/abyssal-player-revival-prep-02124ba7a1cf4026910ce20dc76b9198`, then registered the headless authority fixture in the required runner. Final production and ENet hashes match connected evidence. See `player-life-authority-hashes.json` and `player-revival-enet-report.json` there. Full checkpoint, hosted CI and a normal candidate remain.

Eleventh checkpoint is committed and pushed as `a2bafe05152c4fab168dea494040d3c5030602bb` (`Keep co-op revival authoritative on the host`). Its full hook passed **320 scripts and all 79 gameplay fixtures**, including 65 life-authority checks, in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-1c5a14953e304fdfaa0b1c84194d9c90`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34459367034): 57 watchdog checks, 320 scripts, all 79 exact committed fixtures and 65 life-authority checks, with zero unexpected gameplay errors. The artifact contains 403 log files only, retained until September 17 09:18 UTC. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-a2bafe0-92abb7b95e6c41c7b69c7ac44571957c`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-revival-candidate-f4913386d2d94bbeac16b209699178f4/AbyssalDescent.exe`.
- Build `dev-20260910-090624878-1c1079bc`, SHA256 `C31BFA8ACA4F8E387720EE522A996257891ADC95CD6166F09B044699185D28CB`, 114258032 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-1d2cf32f62f7433b9d01b24262a08f33/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. The accepted prior desktop build remains in place for now, with final normal replacement due before 15:00.

A bounded read-only boss-interruption audit found no justified new defect. Boss damage/delayed attacks execute through the paused enemy physics loop; Lacuna's separate overlays only redraw, while owner teardown removes them. Existing fixtures cover charge cancellation, overlay cleanup and nested reward/defeat pauses. No additional automation, implementation or broad refactor was selected from this audit.

## Checkpoint-eleven batch handoff

All eleven implementations are committed, pushed and verified locally, in normal candidates and on hosted CI. No implementation, review, export or test remains active. Only post-checkpoint updates to this record and the development-plan overview remain uncommitted; preserve them for the next checkpoint. Rest/reward and boss-interruption audits found no justified new scope; do not repeat them or completed verification. Choose a fresh concrete gameplay benefit for the next bounded batch.

The existing 20-minute continuation remains ACTIVE until 15:00 Copenhagen, with new features stopping at 13:30. Final normal desktop delivery and package/hash verification are still required. At 11:14 local, the desktop SHA256 remained `C1DC83BFA6BED7AA73A7C474606ECCF2D7D02BCE29EAF64426CCB62CC5C82288`; the accepted prior build has not been replaced. `main` and the peeled `v0.6.3` commit remain `922a4b576165f6af8477f69bc4c41a48f64916b1`. Branch and upstream match `a2bafe0`; no approval or external blocker remains.

## Twelfth milestone — Room-entry prompts match local readiness

The next room-entry audit found a concrete feedback mismatch. The persistent status hint is always “Move to engage.” In co-op, a player who already signaled readiness remains frozen until all living teammates are ready, yet that hint still asks them to move. The readiness handler intentionally ignores further input at this point. Before readiness, a fresh accepted Attack also starts the handshake, but the hint only mentions movement. The large waiting banner additionally assumes one ally despite the existing larger party support.

Use the existing local HUD refresh to show “Move or Attack to engage” in solo, “Move or Attack when ready” for a co-op player who has not signaled, and “Ready — waiting for allies” once locally ready. Pluralize the existing persistent waiting banner. Carry local readiness and co-op mode into the HUD dictionary; do not add network state, counts, saved fields or alter the readiness/input policy. Existing held movement and release-to-rearm Attack rules remain intact. The host's living-player and departure handling already works and does not need a protocol change for this feedback slice.

`buildcraft_opportunities` owns World/HUD and extensions to the existing input-transition fixture in a fresh disposable source. `encounter_opportunities` owns the actual two-peer waiting observation, focused existing ENet fixture extension and rendered review at 960/1280. `pipeline_audit` independently reviews state ownership/reset/modal behavior. Root owns scope, documentation, review/transfer and checkpoint/delivery. The twelfth implementation is reviewed, integrated and frozen for its checkpoint.

- Production changes are exactly two additive local HUD fields, three state-dependent hint strings and the plural waiting subtitle. Missing fields default to solo; each active survey refresh replaces stale text, new room entry resets readiness and Engage/run-clear hide the hint. Existing modal refresh/input handling is unchanged. All independent reviewers and root found no blocker.
- Focused isolated validation passes **320 scripts**, both guards, **40 input-transition checks, 104 room-entry checks and reward-input checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-189d996a73df40aab0d75f7e18aaf139`. Actual starting-reward handoff rejects still-held Attack; a fresh released-and-repressed Attack engages, Dash alone retains frozen survey behavior, and the next generated door resets the prompt before movement engages. Existing fixture extension is registered already; no new headless test is added for the copy change.
- Actual loopback ENet passes **92 host/94 client checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-518d10f213a64588a6456a996e05af61`. The existing input/room fixture now uses native movement readiness, alternates which owner readies first and checks each owner's actual unready/waiting/Engage/reset text. Waiting keeps actors frozen and wave timers paused while old held Attack/Dash tests remain intact. Timers and explicit Main updates establish the survey wait; this does not claim an actual Pause-menu action or full lobby/public-service acceptance.
- Rendered review passes **eight frames/49 checks**, including teardown, at 960/1280 on RTX 4080: solo, unready co-op, ready co-op and Engage. Both reviewer and root inspected all eight. Full text fits the existing 302px panel, waiting banner agrees and Engage removes the hint. Captures: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-da679f338f0746a9b35beafbc3bbffe0/readiness_feedback_frames`. Screenshots stage a local co-op roster/role; real remote readiness is covered by ENet.
- Root verified and transferred World/HUD, the two existing input fixture extensions and the new render fixture/UID from `C:/Users/mikel/AppData/Local/Temp/abyssal-readiness-hint-prep-a4518a2da6a94640b86421120a2ab694`. Production hashes match focused, ENet and rendered evidence. The render fixture uses the existing generic helper; no new wrapper is needed. See `readiness-feedback-hashes.json` and `readiness-feedback-evidence.json` there. Full checkpoint, hosted CI and a normal candidate remain.

Twelfth checkpoint is committed and pushed as `2aca727aeb0a5d4a15feb9147200378e4e812f0d` (`Match room-entry prompts to local readiness`). Its full hook passed **321 scripts and all 79 gameplay fixtures** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-c531e39c5a884acf9d90771a4d5f0c67`. [Exact-commit hosted validation passed](https://github.com/Excellund/AbyssalDescent/actions/runs/34461617205): 57 watchdog checks, 321 scripts, all 79 exact committed fixtures and 40 input-boundary checks, with zero unexpected gameplay errors. Artifact contains 403 log files only, retained until September 17 09:42 UTC. Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-hosted-ci-2aca727-be92a9cbb5c64652b24a9ec4d59e18e7`.

- Normal candidate: `C:/Users/mikel/AppData/Local/Temp/abyssal-readiness-candidate-fcc91ab196e34697b65e69e82ea02a6c/AbyssalDescent.exe`.
- Build `dev-20260910-093101681-b7f52f57`, SHA256 `EDCAA85E676CE3AA1C33C1463F5768F9271D262C4E1A6B96D6E785290663BF6E`, 114258656 bytes.
- Passed 27 package and 62 native checks with no shutdown warnings: `C:/Users/mikel/AppData/Local/Temp/abyssal-native-smoke-407bc59889c34a24bbb9f8b58c7f68ee/report.json`.
- Source identity: 522 of 524 production files exact, with independently inspected build-ID and debug-disabled overrides; candidate `source-identity.json`. Final normal desktop replacement remains due before 15:00.

## Candidate follow-up — Combat visibility beneath persistent HUD

A bounded actual Main movement probe found a separate gameplay visibility issue: after accepting the initial reward and holding Left, the player naturally passes beneath the persistent Stats panel at both 960/1280, while ordinary enemies pursue into the same footprint. No teleports, enemy staging or invulnerability creates the overlap. Both reviewer and root inspected the two native captures; actors remain partially visible but are darkened beneath the panel and crossed by its text. The panel has a 50% background fill and no contextual fade/compact mode. Tab opens paused Build Details rather than hiding the HUD.

Evidence: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-ad2bc031ad544974aebfc6134d39f82e/hud_occlusion_frames/manifest.json`, with `native_move_left_960.png` and `native_move_left_1280.png`. Native held movement reaches the overlap after 89/140 physics frames; both runs retain 121 HP. The starting reward uses the existing UI signal path, so this is a focused native movement/render probe, not full menu/random-run acceptance. No tracked implementation exists for this candidate.

Consider a small contextual fade of persistent Stats/build panels, including text/chips, only while the local player overlaps their screen footprint during combat; restore on leaving or inspection/modal states. Preserve layout, controls, camera and gameplay rules. Temporary loss of Stats readability is the tradeoff, so confirm active-combat gating, local-owner coordinate conversion and explicit restoration before selecting implementation. `encounter_opportunities` is reviewing those boundaries read-only while twelfth hosted CI completes. Do not start this candidate before the current checkpoint is verified.

Independent read-only review supports this narrow design, with two important boundaries. Use the existing local owner passed to `hud.refresh`; camera/player lookup helpers can switch to an alive ally after death. Convert its footprint through CanvasItem transforms so camera translation/zoom and HUD coordinates agree. The build-strip parent has zero height while its visible content/chips extend below it: overlap must use visible content bounds, even if parent `modulate.a` fades the whole strip. Fade each affected Stats/build parent only; keep status/objective guidance, banners and world health bars unchanged.

Use a local World presentation flag that excludes survey, door selection and `_modal_requires_combat_pause()` as well as ineligible combat. `combat_damage_enabled` alone does not detect Pause or Build Details. Missing state, dead/hidden/removed/invalid owner, modal inspection, room transitions and no overlap must restore full opacity. Suggested acceptance: native movement into/out of Stats and populated build strip at 960/1280, repeated edges, transformed camera/zoom, actual Pause/inspection restore/resume, local-owner isolation and death/next-room reset. Choose faded alpha from real rendered combat frames. No implementation or new test has been selected yet.

## Checkpoint-twelve batch handoff

All twelve implementations are committed, pushed and verified locally, in normal candidates and on hosted CI. No implementation, review, export or test remains active. Only post-checkpoint updates to this record and the development-plan overview remain uncommitted; preserve them for the next checkpoint. The native HUD-overlap finding above and its completed independent review provide a concrete next candidate; no thirteenth production code exists. The current checkpoint is now fully verified, so the next bounded batch can select that scope without repeating completed tests or audits.

The 20-minute continuation remains ACTIVE until 15:00 Copenhagen; new features stop at 13:30. Final normal desktop delivery is still required. Intermediate normal candidates remain in temporary directories, and the accepted prior desktop executable has not been replaced. `main` and the peeled `v0.6.3` commit remain `922a4b576165f6af8477f69bc4c41a48f64916b1`; branch and upstream match `2aca727`. No approval or external blocker remains.

## Thirteenth milestone — Clear combat beneath Stats/build panels

The native HUD-overlap finding and independent review above are now selected for implementation. Fade only each overlapping Stats/build parent while the local owner is fighting underneath it; include child text/chips and restore full opacity immediately outside that condition. Keep status/objective instructions, banners and world health bars unchanged, along with layout, controls, camera, saves and network state. The first candidate uses parent alpha 0.2, subject to actual rendered review. This improves the view of combat beneath the HUD at the cost of temporarily faint Stats/build text; normal readability returns on moving out or inspection.

`buildcraft_opportunities` owns World/HUD and a focused integration fixture in a fresh normal copy of `2aca727`: `C:/Users/mikel/AppData/Local/Temp/abyssal-hud-overlap-prep-ce489df6f9a34bfea40a0b265996d61a`. One local `combat_hud_overlap_fade_enabled` field excludes noncombat, survey, door selection and modal pause. HUD uses the actual local owner and direct CollisionShape2D bounds transformed into viewport coordinates, plus a small margin; cached body radius already includes global scale and must not be scaled twice. Use visible build-chip bounds because the build parent has zero height. Per-refresh alpha assignment avoids accumulating tweens and restores immediately when ineligible.

`encounter_opportunities` owns independent source review and rendered acceptance through native movement into/out of Stats and the populated build strip at 960/1280, including actual Pause and Build Details restoration/resume. `pipeline_audit` independently reviews transforms, ownership and lifecycle. Root owns documentation, source review/transfer, central fixture registration, checkpoint and delivery. The thirteenth implementation is reviewed, integrated and frozen for its checkpoint. Real network behavior is unchanged, so no new ENet fixture is required for this local presentation slice.

- Final parent alpha remains **0.2**, selected from the native combat captures. Actual CollisionShape2D and visible control/chip bounds share viewport coordinates; a 6px body margin starts fading and a further 4px exit margin prevents edge jitter. Stats/build fade independently. Immediate recomputation restores them when ineligible; no tween, timer, enemy scan, input or network state is added. All independent source reviewers and root found no blocker.
- Focused isolated validation passes **323 scripts**, both guards, **34 HUD-overlap, 40 input-transition and 28 combat-pause checks** in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-c4f8e49902c14483bad7a77c8f3a9a10`. The new focused fixture covers native movement around generated columns, actual nested Pause/Build Details, projected body edges with nonuniform camera zoom/owner scale/shape offset, exit hysteresis, populated/hidden chip bounds, nonowner/hidden/downed/removed/missing-owner resets, missing display flag, actual clear/reward/generated-door/survey/Engage and terminal states. Transform/owner boundary cases are explicitly staged; they do not claim a connected-party session.
- Rendered acceptance passes **16 frames/147 checks**, including teardown, at 960/1280 on RTX 4080. Native movement passes below ordinary generated columns, enters Stats, opens/closes Pause, enters the starting build chip, opens/releases Build Details, then moves out. Existing World Escape/Tab handlers are exercised; enemies and terrain remain untouched, and the player receives ordinary damage. No teleports, debug grants, invulnerability or enemy staging create the result. Reviewer inspected all 16 frames; root inspected eight representative combat/modal/restore views and approved alpha 0.2. Captures: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-ebcac36ca2e8411999211d0d08a6659b/hud_overlap_fade_frames`.
- The first GPU fixture route walked directly into a legitimately generated center column and missed its target. Only the walking route changed to go below the columns; production and the natural encounter remained unchanged. This was a fixture routing failure, not a collision or fade defect. Evidence retains that failed attempt and the prior no-fade baseline.
- Root reviewed and transferred World/HUD, the new headless fixture/UID and render fixture/UID from `C:/Users/mikel/AppData/Local/Temp/abyssal-hud-overlap-prep-ce489df6f9a34bfea40a0b265996d61a`, then registered the headless fixture in the required runner. Production hashes match scoped and rendered evidence. See `hud-overlap-hashes.json` and `hud-overlap-gpu-evidence.json` there. The renderer uses the existing generic helper. Full checkpoint, hosted CI and a normal candidate remain.
