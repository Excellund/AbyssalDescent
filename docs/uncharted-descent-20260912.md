# Uncharted descent development — September 12, 2026

The user authorized a new branch and autonomous new feature development until 16:00 Europe/Copenhagen (14:00 UTC), explicitly outside the development queue. Work runs on `codex/uncharted-descent-20260912`, starting at `d3a82a2` with substantial earlier uncommitted work preserved. The original files and hashes are recorded in `.feedback/uncharted-descent-20260912/baseline.json` and `baseline-files/` before implementation.

The authorized workday ended at 16:00. Its scheduled closing wakeup arrived at 16:01:48, and the final read-only handoff check passed at 16:01:59. The heartbeat `new-game-features-until-16-00` is now paused. Feature source remained frozen after verification; no development continued past the deadline. Board items were not claimed, reordered, seeded or implemented. No commit, merge, tag or push occurred; existing working changes remain preserved for a later user-approved checkpoint.

## Completed handoff — 16:02

Eight additions, the Practice recap and unlocked-Vessel extensions, and their Menu integration corrections are implemented. The canonical desktop file contains normal build `dev-20260912-122411473-48c188c1`, SHA-256 `89B9C9C5A71B131384F0691E55B1820725ECCBEBC8595A088DE6B907A467F6F0`. Package verification and the actual executable pass 27/107 checks, with clean shutdown and isolated user data. Four co-op flows pass 353 assertions. The [playtest guide](uncharted-playtest-20260912.md) explains where to find the additions.

The complete final feature source passes all 134 suites, 454-script compilation, both guards and strict shutdown checks in immutable copy `162dfd7caa244dcf93a9b879d5523ee3`. All 1,084 captured inputs stayed unchanged and match primary and the delivered normal build. The final receipt is `full-practice-vessel-result.json`, SHA-256 `102DB12377D4FE57DE3906AC5B0CC796A4D3F0A49F192DFE1C463179E0C11205`. Earlier clean and failed receipts remain preserved.

Feature implementation, full verification and normal build delivery are complete for this increment. The separate ordinary-input pilot is sealed: both declared seeds naturally reached Rest and continued afterward; one chose Recover (122→140 HP), the other accepted the offered First Strike upgrade with unchanged HP. Neither offered Relic Recovery. The pilot retains 42/44 passing checks and two unverified movement metrics; it is not a clean pilot or a human balance verdict. Both preparatory failures and the clean 16-check input preflight are retained. No production/default-fixture changes or gameplay advantages resulted.

The source is frozen and all agents have finished. The final read-only delivery check confirms all 1,084 source inputs, 689 retained log hashes and the desktop hash, with no issues. Board revision 304 remains unchanged; 184 inherited dirty files remain byte-identical, and no original file is missing. No checkpoint commit, push, merge or tag has occurred. The deadline receipt is `.feedback/uncharted-descent-20260912/final-delivery-state-140159996685.json`; the check did not launch Godot or export again. The existing heartbeat was paused through the automation tool. Human playtesting and ordinary-route Recovery coverage remain outstanding; all pilot failures and historical receipts remain intact.

## Feature direction

Prior work emphasizes readable fights, distinctive characters, build choices and reliable complete runs. The first additions address missing ways to learn from runs:

- **Defeat recap:** a bounded record of recent accepted damage, shown on the results screen with concrete damage sources. Preserve combat mechanics, authority and local-player attribution.
- **Run archive:** filter completed runs and inspect the recorded sequence of build decisions. Use existing local history and reward data; support older records without fabricating missing facts.
- **Enemy Field Guide:** fourteen ordinary/specialist enemies, with recognition cues and factual responses, available in Menu and Pause.
- **Relic Recovery:** a new Mission to retrieve three relics and return them through a fight; living co-op players can divide the retrieval and recover a fallen ally's cargo. Host owns pickup, drop, delivery and completion.
- **Rest Site choices:** keep the existing healing or invest the visit in an upgrade to an eligible Boon already owned. Existing caps, Rest route restrictions and Oaths remain; each living player chooses once before the next doors and checkpoint.
- **Oaths This Run:** inspect relevant Oath goals from Pause, including recorded progress, intact requirements, completed conditions and irreversible failures. Reuse existing award rules; saved achievements, legacy uncertainty and host-only co-op evidence stay distinct.
- **Farshot:** a conditional Boon for keeping distance while damage lands, with the same per-target coefficient rules as existing conditional Boons. Includes ordinary draft, Rest, Continue and co-op compatibility.
- **Warden Practice:** a separate solo arena with base Bastion, the actual Warden fight, immediate retries and an in-memory damage recap. Keep the normal saved descent, selected setup, history and progression intact; reject entry during party setup or an active party.

After these increments, reassess remaining time and integration gaps. Existing feedback work and awaiting-playtest changes are excluded.

## Verification and delivery

Use disposable-project helpers and isolated user data for all unattended Godot runs. Focused checks compile the entire project; render new UI at representative sizes and inspect the actual frames. Verify changed network behavior through real host/client fixtures when applicable. Keep development builds out of remote telemetry and leaderboard submissions.

Before final delivery, run the required full regression suite and normal exporter `.github/scripts/export_playtest.ps1` without `-DebugRun`. Replace `C:/Users/mikel/Desktop/AbyssalDescent Playtest.exe`; inspect the package, final hash and isolated native executable check. Record exact evidence and limitations here. Passing automation is not human acceptance.

## Work log

- 10:26: branch and timed continuation created. Board inspected read-only: eight entries await playtest; none are queued or developing. Baseline capture precedes feature edits.
- First batch: [damage recap](defeat-recap.md) passes accepted-damage, checkpoint, revival and real host/joiner checks. Verification exposed and fixed joining-player premature result persistence; final authoritative outcomes now save once, with disconnect/menu/quit fallback coverage.
- [Run archive](run-history-archive.md) passes search/selection/history tests and actual production-canvas rendering. It reuses the damage recap component. Native rendering exposed overly small/blurry text; touched archive/glossary/results panels now compensate for canvas stretch using local crisp fonts and scrollable content.
- [Enemy guide](enemy-field-guide.md) is implemented and verified, including 74 native Menu/Pause frames with readable, correctly weighted keyword text. [Relic Recovery](relic-recovery.md) passes focused gameplay, normal movement rendering, Continue geometry and real ENet. The network check exposed an oversized objective snapshot and a spectator/owner HUD distinction; both are fixed. Independent lifecycle review found no further defects. The [separate live combat smoke](relic-recovery-live-combat.md) passes 14 checks using ordinary controls, active enemies, base Bastion and an actual Mission reward; its headless mouse driver is distinct from the native presentation captures.
- Original working changes remain preserved. `.feedback/uncharted-descent-20260912/audit-changes.py` records this task's delta against the baseline; no baseline file is missing. The board has not been mutated.
- Combined verification: 421 scripts compile; ten scoped gameplay/UI/history/route/checkpoint suites pass 1,691 checks, retaining world/network contract guards. Evidence: `.feedback/uncharted-descent-20260912/combined-tests.log`, disposable project `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-da6f94448f5941e08e2b4f810ff31941`. This is focused integration evidence, not the final full-suite/build delivery gate. Font-face refinement afterward is covered by the guide's final native typography check.
- Independent archive review caught a known starting reward being displayed as unknown depth. Depth zero now remains distinct from missing data, with three additional regression assertions. Feedback board rechecked at revision 304, unchanged since this task began.
- Second batch: [Rest Site choices](rest-site-choices.md) uses the existing capped Boon grant and reward barrier. Final UI verification passes 39 Rest checks, alongside earlier 467 build-inspection, 22,757 layout and input-release checks. Final native Main rendering passes six frames/160 checks, exercises every Boon description and actual keyboard decisions, and has been visually inspected. Focused gameplay passes 307 Rest checks plus 191 descent-presentation and existing Catalyst runtime checks. Co-op review caught a dead player retaining an obsolete choice; the resolver now completes that spectator's barrier without a grant. Native ENet exposed joining-player recovery not reaching the host's HP display, fixed through the existing reward snapshot path. Final native co-op passes 44 host/43 client checks for independent benefits, health/build replication, stale/sender/run checks, death and actual disconnect. Independent UI review also preserved the original normal-reward Build button's font size. Final root UI/archive/glossary checks and 427-script compilation pass in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-27b924b4cd004e90908561b19a1641d0`.
- Full first-batch integration uses the frozen pre-Rest snapshot `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-da82e82d6cbf4f6592f1b4b83b6e71ad`. Import and all 119 default checks ran; 116 passed strict validation and three required follow-up. The inherited Oath/Effigy fixture passed its assertions but needed native audio retirement, now fixed and verified at 29 checks. Brittle Cover passed all 115 assertions but leaked two native resources on the original exit; three targeted diagnostics passed unchanged, so the intermittent issue remains documented, not claimed fixed. Checkpoint lifecycle needs its joining-death fixture to deliver the new authoritative outcome before expecting persistence; that update and missing fixture input binding are being verified. Details: `.feedback/uncharted-descent-20260912/full-batch-one-result.json`. This is not the final full-suite/build delivery gate.
- Review aid: `.feedback/uncharted-descent-20260912/build-review.py` reconstructs a text-only `feature-review.diff` against the captured input, isolating this workday's edits from earlier dirty changes. It validates preserved original hashes and leaves Git, source files and the baseline untouched.
- Checkpoint compatibility follow-up passes 183 lifecycle and 68 damage-recap checks, plus 427-script compilation, in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-a8ba518b31664dd09bd988e1504ad373`. The fixture now delivers the actual authoritative joining-player outcome before checking final history, progress and submission, verifies duplicate/outcome/menu-fallback behavior and initializes the genuine reward input bindings. Production code required no further change. The original full-batch failure remains recorded; no full clean final gate has yet been claimed.
- Rest-dependent integration is complete: alternative-boss selection 254, biome clarity 640, biome room context 1,246, boss atmosphere 139 and Shatter Pillar 20 pass with compilation/guards in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-0eca67511b2d47f1abc377ed83c4cd75`. The existing complete native room-layout/descent fixture passes 602 host/604 client checks in `C:/Users/mikel/AppData/Local/Temp/abyssal-enet-99a6233fde7d404883e2dc90f3dc097f`, retaining its offered/entered geometry, act and shared music checks. Fixture setup now establishes the real run-provenance handshake and resolves each owner's Recover choice before expecting next doors.
- Current batch status: five additions implemented and independently reviewed, feedback board still revision 304 (eight awaiting playtest, no queued/developing work). Continue this same branch through the scheduled workday; do not declare the timed task finished or disable its heartbeat early. Reassess a bounded feature or integration gap next. The full clean final gate, final normal export and native executable verification remain for the workday delivery; the earlier intermittent Brittle Cover teardown must not be represented as fixed. No desktop export, commit, push or release has occurred during this task yet. All three agents are idle and can receive bounded follow-up tasks.
- 11:27 five-feature integration: the complete default suite passes import, 427-script compilation and all 121 checks in the frozen disposable project `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-80f40232904041fb9729f687f7ad4434`. All 424 captured script hashes remained unchanged. Every console log was inspected: no script/parse/compile errors, native-resource-in-use errors or ObjectDB leak warnings; only the helper's known ignored Windows certificate-store message. The Oath/Effigy retirement fix passes 29 checks, checkpoint lifecycle passes 183, Brittle Cover passes 115 without reproducing its earlier intermittent exit failure, Rest rules/UI pass 307/39, and ordinary reward layout passes 22,757. Preserve the original first-batch failures and their follow-ups above; the unchanged Brittle fixture's clean run is not a claimed leak fix. Evidence: `.feedback/uncharted-descent-20260912/full-five-feature-tests.log`, `full-five-feature-snapshot.json` and `full-five-feature-result.json`. This validates the five-feature increment; development continues and the workday's final verification/export remains outstanding.
- 11:34 third increment: [Oaths This Run](run-oath-tracker.md) adds a read-only recorder projection and Pause screen. The independently reviewed data layer passes 1,286 projection, 1,014 existing Oath and 199 movement checks, compilation and both guards in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-f2ebc99c082f4fe8b58ea58656adeb4a`. Review caught the real roster helper falling back to an ally when the local avatar is absent; build evidence now verifies the actual owner. Initial UI verification passes 102 checks plus combat pause 36, checkpoint lifecycle 183 and glossary readability, with 432 scripts compiling in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-1278ee7bccb642f49842520f61aa1bd1`. Numeric progress display and resize/empty-state refinements followed; final UI/native review is ongoing. No award, persistence or networking behavior was added.
- Two pre-registered [ordinary first-act pilots](ordinary-first-act-evidence.md) retained actual defeats: seed 12092026 cleared two rooms and died in depth-2 Suppression; seed 12092027 cleared three rooms and died in depth-3 Vanguard. They passed 26 fixture invariants with ordinary earned rewards, active enemies, enabled damage and real input. Neither Recovery nor Rest was offered before defeat, so this does not establish ordinary-progression coverage of those additions. No retries, combat-bot expansion, balance verdict or production changes resulted.
- Oath native review is complete: 18 actual Main frames and 1,775 checks pass at 960×540/1280×720 with the production canvas. All frames were inspected, including the live recorder, separately labelled staged broken/earned/legacy/joiner evidence, visible keyboard focus, the earned filter, scrolling, the save notice and existing Options/Glossary navigation. Review exposed overlapping underlying Pause actions and an undrawn row focus style; final captures verify both corrections and clearer checkbox icons. Native import/render exit cleanly, with no ObjectDB or resource warnings. Evidence: `.feedback/uncharted-descent-20260912/run-oath-tracker/` and `run-oath-native-final.log`; the frozen source precedes Farshot edits. This completes this increment's native UI review; timed development and the final workday gate continue.
- The native Pause review also exposed very small inherited Options copy. The bounded [Options follow-up](pause-options-readability.md) preserves settings callbacks while adding 18-pixel physical copy, a scrolling body, fixed title/Back, readable popup fonts and visible tracks/toggles. Final validation passes 103 Options and 161 Oath UI checks, 439-script compilation/guards, plus eight inspected actual Main native frames and 59 interaction checks. Both display/resolution popups open and cancel by keyboard; isolated volume callbacks change and restore their setting. Earlier fixture/parallel-source failures remain documented with final clean evidence under `.feedback/uncharted-descent-20260912/pause-options/`. Production source is frozen for the next integrated snapshot; documentation and evidence were the only remaining edits.
- 11:53 seventh feature: [Farshot](farshot.md) adds a capped conditional Boon through the existing accepted-damage resolver: +10 Damage basis per pick, up to three, when the actual foe center is at least 160 units from the owner's current body at impact. Independent source review retained actual-target conditions, raw/coefficient descendants and normal authority. Focused verification passes 34 Farshot, 37 acquisition/Continue/Rest, 87 shared boundary, 48 keyword lifecycle, 465 description, 231 glossary, 314 Rest and 68 Effigy checks, plus 439-script compilation/guards. Real loopback ENet passes 32 host/four client checks; initial tuning still needs human playtesting. Native reward/build review is ongoing.
- Native new-card checks exposed a wider presentation gap: ordinary rewards still shrank with the 2560 canvas, while Rest already used physical sizing. The shared reward content now uses physical dimensions and crisp local fonts for every mode; Build Details uses 18px local fonts. At 960×540 three offers use columns, while four retain the required two-by-two grid with reduced leading and compact header/footer. At larger sizes the existing arrangement remains. The dense layout preserves complete descriptions, figures and boss epitaphs. After one typed-array compile correction and a measured first layout failure, the expanded whole-roster fixture passes 28,419 checks across 960×540/720, 1280×720 and 1920×1080; Build inspection 467, Rest UI 39 and existing reward/queued-input suites pass. Evidence: `.feedback/uncharted-descent-20260912/reward-physical-layout-third-tests.log`, disposable project `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-f3e94a94f0ea472a803f93ac0732031c`. Final native checks include the densest four-card Arcana/Boss cases. Responsive Pause Options is separately being verified after native inspection exposed its old tiny controls.
- 12:00 native completion: [Farshot UI](farshot-native-ui.md) passes 35 rendered frames and 1,039 checks at 960×540, 1280×720 and 1920×1080, including every offered/owned level, Rest, Build inspection and the densest four-card Arcana/Boss layouts. All categories were visually inspected; root additionally reviewed the dense pages, normal three-card draft, Rest and Build frames. Readable 18px glyphs and reduced short-grid leading have no observed collisions. Final source-matching project: `C:/Users/mikel/AppData/Local/Temp/abyssal-gameplay-render-f06de1723bdd4acbb2972d2af6cc3270`; log `farshot-native-complete.log`. Earlier native test assumptions were corrected for offscreen Windows mouse coordinates and subpixel rectangle boundaries; the actual Rest title/body gap was fixed in production. Initial failures remain retained.
- A complete seven-feature integration run is now in the immutable disposable project `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-7e736f5ae48247f7810896127d78edd5`, with 126 default checks and 1,050 captured input hashes. Import, 439-script compilation and guards passed; remaining runtime checks are ongoing. After that snapshot, the editor's debug encounter dropdown was corrected to expose the already-implemented Relic Recovery option at appended index 29. Static verification confirms 30 matching labels/enum entries; defaults and serialized earlier IDs are unchanged.
- Board inspected read-only again: revision 304, the same eight awaiting-playtest entries, no queue mutation. Audit still finds no missing original file; 184 inherited dirty paths remain byte-identical. No normal desktop export, commit, push, merge or tag has occurred. The workday remains active through 16:00; final full verification and canonical normal build remain outstanding.
- Seven-feature integration completed all 126 registered suites in the unchanged frozen copy: 125 pass; reward availability failed 341 of 949 assertions after its fixed headless pointer used the old logical card coordinates. All 1,050 captured input hashes remained unchanged and 127 raw console logs were retained and hash-checked. The fixture now converts that fixed pointer into the physical reward layout's canvas coordinates; actual hover, Attack confirmation, World grants, exhausted pools and disk Continue remain exercised. Its separate current-source follow-up passes all 949 checks, 439-script compilation and guards in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-553dec2740cf439183c10b9aafd5e8a3` (`reward-availability-coordinate-tests.log`). The original full-run failure remains in `full-seven-feature-result.json` and `full-seven-feature-logs`; this is an increment with a verified fixture correction, not a clean final workday gate.
- 12:12 eighth feature in development: a fixed Warden practice arena using actual base Bastion and Warden behavior, with pause, immediate Retry and Menu. Feasibility review rejected Main/debug reuse because those paths can read, clear or record normal runs. The separate scene must prove unchanged normal save/profile/history bytes through death, victory, retries and return, then successful normal Continue. Menu entry and readable Practice controls are being developed separately from runtime and independently reviewed. No additional boss variants, loadout editor or network practice are in this slice.
- The shared reward layout's remaining [Mission coverage](mission-reward-readability.md) now passes 36 native frames and 1,179 checks: six real bonuses, three/four offers and all three physical window sizes, including ordinary keyboard/World grants. Representative frames were visually inspected. The review found a two-pixel title/banner rectangle overlap at 1920; explicit spacing fixes it, and bonus copy now uses 18px. Expanded registered layout checks pass 31,209, plus Rest UI 39, Mission bonus 224 and 445-script compilation/guards. A separate initial import emitted Unicode surrogate diagnostics and was strictly rejected despite exit zero; strict UTF-8 decoding passed all 884 source/frozen scripts and the fresh-copy follow-up imported cleanly. Both the earlier native layout failure and intermittent import receipt are retained; no engine diagnostic fix is claimed.
- A further read-only gap audit recommends completing and verifying these eight additions before opening another system. A live route chronicle would need new persisted/co-op route evidence and overlaps the new History/Oaths/Build tools; a Practice damage breakdown is a possible later extension. Neither has been started or promised. Continue useful integration and native verification within the authorized workday; the normal desktop build and timed stop at 16:00 remain outstanding.
- 12:38 [Warden Practice presentation](warden-practice-ui.md) is frozen after 233 focused UI checks, 446-script compilation/guards, and 34 inspected native frames with 251 passing checks. Actual Menu input covers normal and Continue entry, preserved corrupt-checkpoint error controls, pause/resume/retry/results, party denial, scrolling and return focus at 960×540, 1280×720 and 1920×1080. Native review found a one-pixel focused Exit clipping issue and an unnecessarily small arena beneath the first HUD; the final bounded scroll correction and 240-pixel sidebar resolve them. The original failed receipts remain documented. Actual Warden charge/cleave/nova warning renderers are inspected at 960, with staging explicitly recorded; the separate active-combat fixture establishes ordinary attacks. Final source and all 34 image hashes are retained under `.feedback/uncharted-descent-20260912/warden-practice-ui/`. The complete eight-feature source has been released for its immutable integration run; this is not a final workday or human-acceptance claim.
- Practice runtime verification passes 106 isolation/lifecycle checks and 31 separate live-combat checks in `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-2ba23f67328449d0bb180c61a4698ee6`, with 446-script compilation/guards. The actual fight records an ordinary base-Bastion defeat after 18 Attacks, four Dashes and all three Warden patterns; no combat advantage or human balance verdict is implied. Seeded normal checkpoint/profile/history/settings bytes and nonempty Catalyst/Ascension setup remain intact through Practice and successful normal Continue. A real offline SceneMultiplayer peer had incorrectly blocked normal Menu entry; the narrow session predicate fix also passes 75 join/cancel checks and real ENet 12 host/37 client checks.
- 12:50 full eight-feature integration finished all 128 default suites in the immutable copy `C:/Users/mikel/AppData/Local/Temp/abyssal-validation-e305a2fb1ef142a0bc80d77a34e25f93`: 127 pass, with Practice's 106 successful assertions followed by an ObjectDB/one-resource shutdown diagnostic. All 1,066 captured input hashes remained unchanged and matched current source; all 129 console logs are preserved and hash-verified in `full-eight-feature-logs/`. The remaining 75 checks continued in that same source copy and passed. This is not a clean full-suite receipt. Failure-time user data was not captured before continuation, so it is not presented as preserved failure state.
- Bounded shutdown investigation reproduced no leaked type or cause: five independent/reused-profile verbose Practice runs and all 53 preceding-through-Practice checks passed unchanged. The original failure remains retained in `full-eight-feature-result.json`; `practice-retirement-diagnosis.json` records the separate clean observations without claiming a fix. One fresh full confirmation run is underway, with Practice-only verbose logging and a pre-Practice isolated-data snapshot. Production source remains frozen during that check and independent lifecycle review.
- The normal executable probe now exercises genuine Menu input into Practice, Pause, Retry and Menu before its existing fresh tutorial/normal-run checks. Its new assertions require unchanged isolated profile files/setup and restored normal menu state. Compilation and independent review pass; runtime verification against the normal export remains pending. A normal desktop export started at 12:52, using the canonical exporter and its unique internal dev identifier, production autoload/package checks and isolated staging. This advances delivery preparation before the final hour; timed work and the 16:00 stop remain active.

- 12:55 normal desktop delivery: `C:/Users/mikel/Desktop/AbyssalDescent Playtest.exe` now contains normal build `dev-20260912-105202271-cd352d51` (124,526,376 bytes), SHA-256 `277CEA2B340DC714A86C077078BDFBF2A773979327B82EC42B762D5F16E8CCD1`. Import, release export and embedded-package verification pass. The package probe passes 27 checks; the actual release executable passes 78 checks for fresh Menu, Practice entry/Retry/return, ordinary tutorial movement, Menu return and a second normal descent. The external driver uses isolated data/services, checks the actual release template, retires its application cache and reports no shutdown warnings. It does not open the player's profile. The delivered file, verified staging executable and native test copy have identical hashes.
- Export/source audit verifies all 998 unchanged staging inputs against the frozen full-suite source, with only the three intended Main/project/BuildInfo transformations; 65 workflow helpers are deliberately absent from staging. All 1,066 current source inputs still match the frozen capture. Fourteen native/export logs and JSON results are retained and hash-verified under `normal-build-first-evidence/`, with `normal-build-first-receipt.json`. The full confirmation gate and final co-op regression remain pending; no human acceptance or end-of-workday claim is implied. Independent integration review found no actionable issue in Practice ownership/Continue, Rest stale/barrier paths or Farshot damage descendants.
- Final co-op regression passes 353 assertions through real loopback ENet and production RPCs: Rest 44 host/43 client, Recovery 75/73, Farshot 32/4 and defeat recap 41/41. All 1,066 primary inputs match the frozen confirmation source; each disposable co-op copy matches its 1,000 runtime inputs. No retry or source edit was needed. Evidence: `final-enet-manifest.json` and per-flow `final-enet-*` logs. These fixtures use scripted setup and suppressed service startup; they do not establish Internet lobby or human playtest coverage.
- The fresh confirmation run reproduced the Practice shutdown diagnostic, now identifying `AudioStreamPlaybackMP3` and Menu resource `res://music/msx1.mp3`. Both pre- and post-failure isolated user data are preserved with hashes. Practice still passes all 106 assertions; the rest of the immutable suite continues separately. Diagnosis traced Menu starting music before applying its saved muted setting, creating then immediately detaching playback ahead of native audio retirement. A narrow startup-order correction is authorized with real muted/unmuted/resume/profile-preservation checks; it must retain the shared timeline and normal later-unmute behavior. No broad audio refactor or warning suppression is planned. The previous normal build remains available, with a corrected export/native check to follow verified source changes.
- 13:11 [Menu transition follow-up](menu-panel-transitions.md) fixes two reproduced new-screen integration defects: stale outgoing callbacks hid rapidly reopened Guide/History panels, and physical Window resize retained their old inverse canvas scale. Per-panel transition retirement and a deferred viewport resize layout now preserve visible panels and current centered bounds. The original 16 diagnostic frames remain retained alongside 16 corrected native frames; representative before/after pixels were inspected. Focused transition 66, Practice UI 233, checkpoint Menu 72 and panel fit 211 pass with 448-script compilation/guards. The initial audio timeline assertion failure remains recorded and links the separately verified audio guard correction. With that final audio guard included, actual Menu/Practice replay passes 34 frames/251 checks at 960/1280/1920, preserving checkpoint recovery and keyboard return behavior. Source/fixture/UID/runner are frozen for the final 130-suite gate; evidence and exact source differences are in `.feedback/uncharted-descent-20260912/menu-panel-transitions/final-receipt.json`. This is an integration receipt, not a final workday or human-acceptance claim.
- 13:12 refreshed normal desktop delivery contains the verified Menu startup/exit and panel-transition corrections: `dev-20260912-111043832-7c213a2e`, 124,528,424 bytes, SHA-256 `02C26CCEF9796C268D2328B14868D4C6CF40A22F65C84C968D055E750C4A33D0`. Export/import/package checks pass; 27 package checks and 78 actual release-executable checks pass with clean shutdown in `abyssal-native-smoke-437f956216144809857065bed27dfbb4`. Source staging matches all 1,002 unchanged inputs, with the three intentional normal-export transformations; all 1,070 current inputs match `full-eight-final-source-before.json`. Fourteen raw evidence files are preserved in `normal-build-eight-feature-evidence/`; `normal-build-eight-feature-receipt.json` ties their hashes to the desktop and both identical executable copies. Previous build evidence remains intact.
- The final eight-feature full gate now uses frozen copy `abyssal-validation-4ff28755b6c048e1a5723f10b043697d`, 130 default suites and 1,070 exact input hashes. It includes the new Menu audio and transition regressions and documented Practice-only verbose logging. Runtime checks are ongoing. Root additionally inspected corrected native rapid-reopen Glossary and resized History frames. A read-only design assessment is considering a small in-memory Practice damage breakdown after this increment has a clean gate; no next feature implementation has started or been promised. The timed task remains active until 16:00.
- 13:19 Effigy fixture instrumentation identifies the separate shutdown cause: its reused impact/swing players start 17 native playbacks, while the tree-exit observer sees only six final handles. Two missed weak handles match the exact verbose leaked OggVorbis playback IDs. The existing fixture feedback double will observe each real start and wait for those handles as well, preserving gameplay assertions and production sound behavior. The bounded Practice recap prototype can proceed independently in a separate disposable copy; primary integration remains conditional on a clean eight-feature gate and remaining time.
- 13:20 [Oath fixture retirement](oath-fixture-retirement.md) resolves the final gate's reproduced teardown failure without changing inherited Oath/Effigy mechanics. Exact-source verbose replay identified native Ogg playback handles retaining the impact/swing streams; the exit-only observer saw six latest handles while 17 real playbacks were created. Instrumentation matched two missed live handles directly to leaked IDs. The test double now observes each actual sound start, retaining all 29 existing assertions and adding three replaced-handle/release checks. Disabling just those observations in a disposable counterfactual fails the new assertion and reproduces the four-resource warning. Final corrected verbose and fresh isolated focused validation pass Oath/Effigy 32, shared attack 38, 448-script compilation and guards; only the fixture changed. Original failures, source/input hashes, the typed-WeakRef correction and clean evidence remain in `.feedback/uncharted-descent-20260912/oath-retirement/`. The ongoing immutable full-suite remainder remains separate; no clean full-gate claim is inferred from this focused correction.
- 13:37 clean eight-feature gate: all 130 default suites, import, 448-script compilation and strict diagnostics pass in `abyssal-validation-aeb401a85d1d46f2b4acb695d16d909c`. All 1,070 captured inputs remain identical in both the frozen copy and primary. The 524 raw logs and 131 console logs are retained with hashes; `full-eight-clean-result.json` has SHA-256 `39E554C9B9FB08BACC369284932E559A92097FC7D1C0153C81515FFB1B40B1BA`. Effigy 32 and Practice 106 pass verbose checks at the former failure points, alongside Menu audio 19 and transitions 66. Preserve earlier incomplete/failing gates; this is the first clean full eight-feature gate.
- 13:38 [Practice damage recap](practice-damage-recap.md) integrates 13 exact verified files after the clean gate, with five prior files backed up and the integration receipt preserved. The attempt-local recorder consumes each actual accepted-damage sequence once before fatal results appear, keeps six recent events, distinguishes armor-adjusted HP loss and clears on Retry. The unchanged live Warden combat fixture now passes 35 checks, including its real final damage source. Prototype verification passes new core 74/UI 974, existing Practice core 106/UI 233 and shared recap 68 checks, plus 451-script compilation/guards. All 20 native frames and 524 presentation checks pass; staged visual damage is documented separately from live combat. Root inspected full and empty 960-pixel panels and the full 1920-pixel panel.
- 13:40 integration follow-up corrects only the native probe assertion label from `loses24HP` to `loses 24 HP`; exact byte replacement is verified and separately recorded in `practice-recap-native-probe-label-update.json`. Probe SHA-256 is `CD8A8ECE76276042DDE4DC768D0F0ECC97DBD28F8AFBA62B18FB5DF1BBA01429`. The original integration receipt and pre-label file remain intact. The expanded native probe expects 83 checks, including explicitly scripted accepted damage in Practice; actual release execution is still pending the refreshed build. Combined primary focused checks run before the final 132-suite capture and export.
- 13:45 integrated primary verification passes all seven focused suites and 1,509 assertions, with 451-script compilation and both source guards. All 1,077 inputs match the exact disposable copy `abyssal-validation-68c284bed2174b6bba96fd20af09a81b`; no primary or copied source drift occurred. Evidence: `practice-recap-primary-result.json`. The complete 132-suite gate now runs against the same captured inputs in `abyssal-validation-50395190fdca4efba64087e326ca753b`; source manifest SHA-256 `AF26C1BB409EBF46E661BAE1D2BC5DEB0B78F3ED6DD63C3B7EA3FA02662AB633`.
- 13:49 refreshed normal desktop delivery includes the integrated Practice recap: `dev-20260912-114634183-44525c03`, 124,531,672 bytes, SHA-256 `0034DE7676915BEA176745B298DA22A73B2F30B990ADB4F40E4C36C74EC6AA63`. Normal import, release export and package verification pass; package 27 and actual release-executable 83 checks pass with no shutdown warnings. The native copy is `abyssal-native-smoke-d56c3504304041d4b91c63ea8f68a470`; it exercises fresh Menu, Practice Pause/Retry/return, normal tutorial movement, Menu return and a second normal descent with isolated user data. The additional recap boundary setup is explicitly scripted: incoming 25 from `warden_nova` produces 24 actual HP loss through base Bastion armor, shown in the panel and cleared by Retry. It is separate from the actual active-Warden live-combat evidence.
- `normal-build-practice-recap-receipt.json` verifies identical desktop/export/native executable hashes, all 1,077 current source inputs matching the 132-suite capture, 1,008 unchanged export inputs and only the three intended normal-build transformations. Fourteen raw logs/JSON results are retained and hash-verified in `normal-build-practice-recap-evidence/`. The earlier desktop receipts remain intact; the new complete 132-suite result is still pending. Source audit records 44 changed original files, 126 added files, zero missing original files and 184 inherited dirty files byte-identical to the captured input. Branch and base HEAD remain unchanged; no commit, push, merge or tag has occurred.
- 13:58 complete Practice recap gate is clean: all 132 default suites, import, 451-script compilation and strict shutdown checks pass in `abyssal-validation-50395190fdca4efba64087e326ca753b`. All 1,077 frozen inputs are unchanged and match primary. The 532 raw logs and 133 console logs are retained and hash-verified. `full-practice-recap-result.json` has SHA-256 `0E406553732EF13F306F711D7A5C099E9D820A7598D0F51AD558163E7309EBEC`. The prior clean 130-suite receipt and all failed historical receipts remain intact; the current normal desktop receipt matches this clean 132-suite source.
- The final source/copy review found no additional wording or discoverability defect. Two stale technical-document passages now reflect temporary Practice recap capture and the completed integration while retaining historical prototype evidence. The feedback board remains revision 304: eight awaiting playtest, 34 done and one archived; no queued/developing items and no mutation during this task.
- 13:56–14:03 remaining feature window: feasibility found no missing runtime interface for the other four Vessel passives in Practice. A bounded selector prototype is authorized in `abyssal-practice-vessel-prototype-8b1ebfc3d8db4a7db7561d84c2a9ee6d`, separate from primary and the delivered build. Unlocked IDs are read through a deep copy because the normal getter normalizes its input and caches; normal selection callbacks must not be reused. Next-attempt choice persists across Resume/Pause/defeat and applies only to a fresh actor on Retry, preserving the current fight and avoiding inherited Bastion armor. Core/passive/isolation and UI/native work run in parallel; integration requires a reviewed combined pass and time for final verification/export. No queued mechanics are in scope.
- Prototype exported-probe coverage is being extended separately: explicit Hexweaver unlock setup in the isolated native fixture precedes its byte-preservation snapshot; both existing Bastion attempts remain, followed by keyboard selection, Resume with the same actor, and Retry as fresh base Hexweaver. Expected coverage is 106 checks and three attempts, with scripted setup identified in metadata. One initial UI snapshot caught a root probe local-variable type inference error; the declaration was corrected to explicit `int` and that failed receipt is retained. No primary probe or desktop build was changed by prototype work.
- 14:13 [Practice Vessel choice](practice-vessels.md) prototype passes 454-script compilation/guards and 552 focused core assertions: 326 new choice/lifecycle/passive checks, 106 existing Practice, 52 character-passive and 68 Effigy checks. The actual base packages and passive actions run against the real Warden, including Dash movement, accepted Sigil/Surge damage and Effigy placement/recall. A late-child-teardown guard in Practice fixes a real direct-close path without changing shared Player code or attached Menu/Retry cleanup. Independent review verifies owned service unbinding, actor/action/controller retirement and unchanged profile/context evidence. Initial fixture seed and input-boundary mistakes remain documented; actual idle/physics input timing was corrected without changing mechanics or weakening assertions.
- [Practice Vessel presentation](practice-vessel-ui.md) passes 4,290 focused assertions (3,083 selector, 974 recap and 233 existing Practice), 454-script compilation/guards and 39 native frames with 1,326 checks. All five names and six complete recap rows fit at 960/1280/1920 with unchanged arena geometry/sidebar width. Root inspected the full Effigy Keeper modal at 960/1920 and open popup at 960; the UI owner inspected all 39 frames and the final notice refresh. The notice now explicitly preserves the normal run setup. Native navigation uses measured focused-item Up/Down instead of an unsupported Home/End assumption; the failed earlier fixture run is retained.
- 14:19 temporary normal prototype export passes package 27 and actual release-executable 107 checks with three Practice attempts and no shutdown warnings. The first two Bastion attempts remain; a fixture-owned Hexweaver unlock is explicitly seeded in isolated profile data before the preservation snapshot, then actual popup keyboard, Resume, Pause and Retry create a fresh 75-HP/28-Damage/zero-armor Hexweaver. Recap resets, normal selected setup and saved bytes remain unchanged, and normal first-descent movement/Menu/reopen still pass. Temporary build `dev-20260912-121355999-e739d80a` has SHA-256 `4B9598368B5D06DBBAFD80CFA1DE3F9ED7445799FBF796BF74D17326A30AFDB0`; it did not replace the canonical desktop file.
- Prototype receipt `practice-vessel-prototype/native-prototype-result.json` verifies 1,178 combined code/UID/config inputs, unchanged primary source and desktop hash, identical prototype/export/native artifact hashes, and 14 retained raw logs/JSON files. Its export preset is copied exactly from primary because the disposable gameplay copy omits that packaging input. Four intentional staging transformations are checked exactly, including template/Windows metadata in the added preset alongside the usual Main/project/BuildInfo changes. The original staging failure and initial receipt check that omitted preset transformation remain identified in the retained workflow; no game-source correction was involved.
- 14:21 guarded integration copies 15 exact reviewed delivery files after the clean 132-suite primary gate and passing prototype native receipt. Six existing files are backed up in `practice-vessel-integration-backup/`; `practice-vessel-integration.json` records all baseline/target hashes. The combined patch also reproduces all 15 hashes in a fresh copy. Primary now begins combined focused verification, followed by the prepared 134-suite gate and normal desktop refresh. No original baseline file is removed, and no board mutation, commit, push, merge or tag occurs.
- 14:23 integrated primary verification passes all 10 focused suites and 4,964 assertions, with 454-script compilation and both guards in `abyssal-validation-e99418962cec4f8db318de6813e6ae05`. All 1,084 source inputs and copied bytes remain exact. The unchanged live Warden smoke still records 430 frames, 450 damage dealt and 130 actual HP lost; it is distinct from the controlled passive integration fixtures. `practice-vessel-primary-result.json` preserves exact source and raw logs. The expanded 134-suite gate starts from the same inputs in `abyssal-validation-162dfd7caa244dcf93a9b879d5523ee3`; source manifest SHA-256 `33BC95E6767798D9F0C2C8B198F0A18A596EDE11BB4A3B75A574AE5F88EFD477`.
- 14:29 canonical normal desktop delivery now includes unlocked-Vessel Practice: `dev-20260912-122411473-48c188c1`, 124,536,104 bytes, SHA-256 `89B9C9C5A71B131384F0691E55B1820725ECCBEBC8595A088DE6B907A467F6F0`. Normal import, release export and embedded package checks pass; package 27 and actual release-executable 107 checks pass with three Practice attempts and no shutdown warnings. Native evidence is `abyssal-native-smoke-75b8af92df864844a64a43946664f404`. The actual selector/Resume/Retry path, fresh Hexweaver package, saved state and subsequent normal tutorial/Menu/reopen all pass using isolated data and services.
- `normal-build-practice-vessels-receipt.json` ties the identical desktop/export/native executable hashes to the current 1,084-input source capture, 1,014 unchanged staging inputs and the three intended normal build transformations in that capture. Fourteen raw logs/JSON files are retained and hash-verified in `normal-build-practice-vessels-evidence/`. The 134-suite full result is still pending, while prior build/gate receipts remain intact. The latest source audit reports 44 changed originals, 135 new files, no missing originals and 184 inherited dirty files byte-identical to the original captured baseline. A documentation sentence was clarified to identify the Warden as the recipient of passive damage; no runtime or fixture source changed.
- A bounded ordinary-input coverage pilot now targets the unresolved natural-route gap in the earlier first-act evidence. It keeps seeds `12092026` and `12092027`, starting draft, offered-door/reward priority and 21,600 control-loop iterations per attempt. Reward presentation waits are additional; this is not a strict six-minute elapsed-time limit. Only a separate disposable driver's movement/spacing and responses to currently visible enemy/projectile/telegraph geometry change. It cannot force routes, grant powers, heal, teleport, suppress enemies or inspect future offers/timers. Both outcomes will be retained without replay or post-outcome tuning; no production, default fixture or board files change. This checks functional progression and cannot establish human difficulty or balance.
- 14:36 final feature gate is clean: all 134 default suites, import, 454-script compilation, both guards and strict diagnostics pass in `abyssal-validation-162dfd7caa244dcf93a9b879d5523ee3`. All 1,084 copied/frozen/current input hashes remain unchanged. The 540 raw logs and 135 console logs are preserved and hash-verified; `full-practice-vessel-result.json` has SHA-256 `102DB12377D4FE57DE3906AC5B0CC796A4D3F0A49F192DFE1C463179E0C11205`. This gate matches the current normal desktop build's frozen source; earlier receipts remain unchanged.
- The ordinary pilot's first registered invocation stopped before combat: its inherited pointer/click routine could not resolve the starting draft under the current physical reward layout. Zero completed attempts and zero combat/route outcomes are recorded, and only the two verified owned isolated processes were stopped. That failure is preserved separately. One noncombat input preflight and one corrected I/O-only continuation are authorized, retaining both seeds, strategy and offered-choice priorities. The protocol/lock amendment must identify this as recovery from a pre-combat harness failure, not a gameplay retry; no later outcome permits another replay or strategy adjustment. Primary/default fixtures and the desktop build remain unchanged.

- 14:48 ordinary-pilot preparation diagnosis is corrected explicitly: both tested input methods reported exactly the requested root mouse position, disproving the initial pointer-mapping hypothesis. The headless root had a 64-pixel physical window; physical reward normalization consequently put card centers outside the window. Only the disposable driver now establishes a real 1280×720 root and production 2560×1440 canvas before Main. Separate preflight-only corrections use a type-safe focus lookup and assert the real initial Skirmish transition rather than expecting doors. Neither preparation reached combat. The corrected preflight passes 16 checks, 455-script compilation and both guards in abyssal-validation-bfaef48a373b4bc4a7b111fae1d3b7c2; original mouse input, three real card hovers and RNG preservation pass. Independent review agrees. One explicitly locked corrected combat invocation is running with both original seeds and frozen strategy. Seed 12092026 has naturally visited Rest at depth 4, selected Recover through the actual offered UI, healed 122→140 HP and continued before defeat in depth-6 Fortress after 62.15 game seconds. No Recovery visit occurred in that first outcome; the second remains pending. Pilot work is bounded to 15:15, with no gameplay-outcome retries or tuning.

- 14:53 the bounded ordinary pilot is sealed after both declared combat outcomes. Seed 12092027 naturally reached depth-4 Rest, accepted the actual offered First Strike upgrade (+16→+32 conditional Damage on its card), retained 112/130 HP and continued before defeat at depth 7 after 82.92 game seconds. Compact snapshots retain IDs rather than numeric post-upgrade properties. Neither seed offered Recovery. The strict pilot receipt retains 42/44 passes and two movement maxima without their per-frame coordinates. Independent review finds that the sampler uses unchanged network room IDs across normal Rest position resets, but cannot conclusively attribute the exact maxima from retained observations. No failures were deleted or retrospectively converted to passes, and no combat outcomes were replayed. The [ordinary evidence](ordinary-first-act-evidence.md) preserves both original defeats, preparation failures, corrected preflight, final outcomes and raw hashes.
- Final playtest-guide review corrects three concrete claims: recap entries are damage events rather than distinct sources; Rest offers up to two owned eligible upgrades only when available; Recovery uses walk-over pickup and receiver entry, and its Mission reward grants Fortified when the Boon is chosen. Practice and Farshot copy match the implementation. Only documentation changed.
- Read-only delivery-state verification at 14:51 passes exact source-set and 1,084 input hashes, the retained full/native receipts and 689 raw/console log hashes, the canonical desktop hash, branch/base HEAD and original-file preservation. Board revision 304 retains eight awaiting-playtest, 34 done and one archived; no queue mutation occurred. Evidence: delivery-state-125140034342.json. The new ignored verify-delivery-state.py --final provides the deadline handoff check without launching Godot, exporting or altering game files. Its final mode refuses execution before 16:00.
